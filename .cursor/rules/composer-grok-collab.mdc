---
description: Dan standing default — risk-based Composer/Sol implement + conditional Grok lanes (D-43/D-65).
alwaysApply: true
---

# Composer ↔ Grok collaboration (D-43 / D-65)

**Dan standing preference (every session):** Risk-based build lock per `model-gates.mdc` § Risk-based routing (D-65). Do **not** auto-stack Solution + UI Playwright + Build Checker + Ship on routine T1. When a Grok lane **does** fire, peer-agree + fix-closure still apply (D-34 — trigger trim ≠ skip agreement). Platform parity (D-20/D-32).

## Phases (only those the D-65 lane table requires)

| Phase | Who | When |
|-------|-----|------|
| **1 — Agree** | Grok **Solution Verifier** | Only if multiple paths / unclear SoT / ambiguous data model / multi-file diverge risk. Else `solution-verifier: N/A (…)`. **conf ≥ 97%** always (D-47). |
| **2 — Implement** | **Simple UI / non-UI → Composer**; **visual-judgment UI → Sol** (Task → `gpt-5.6-sol-high` **directly**; parent Medium → inline). Grok only after D-50 stuck Agree. | Always for product builds. **D-51** before first visible UI edit. |
| **2b — UI judgment** | Grok **UI Playwright** (optional) | Prefer judgment-from-log after worker `verify:*` PASS; live re-run only per D-45/D-65 criteria. Simple UI usually N/A. |
| **3 — Pre-commit** | Mechanical build/verify; Grok **Build Checker** only when judgment needed | Not to re-run green `npm run build`. |
| **4 — Ship** | Composer commit/push/deploy; Grok **Ship Verifier** when required | Tiny-fast-safe FE → Ship N/A; run `ship:evidence-preflight` before any Ship Task. High-risk keeps D-38 + Ship. |

**Ship lock (hard STOP):** Grok **never** commits, pushes, or deploys — except D-50 Phase B product edits after stuck-task Agree (still no ship lock transfer).

## Implementer lock (hard STOP)

- **Simple UI** (wording, labels, column swaps, obvious CSS, straightforward visibility): **Composer**.
- **Visual-judgment UI** (theme, contrast, dark/light, a11y, complex layout, major redesign, visual consistency): **Sol** — Task uses `gpt-5.6-sol-high` directly (do not attempt Medium as Task). Silent Composer on visual-judgment = **NOT RUN**.
- **Non-UI:** Composer.
- **D-50 stuck carve-out:** Grok implements stuck scope after Agree until ship closes.
- Non-Composer/non-Sol parents MUST Task Composer or Sol High — never `resume: "self"` except D-50.

## Pre-edit gate ordering (D-48)

When Solution Verifier is required, PASS/N/A **before first repo write**. Workers must not defer Agree to parent.

## Evidence

Include evidence lines **only for lanes that fired** (or valid N/A). Do not invent Solution/Build/UI-PW/Ship Task ids for skipped lanes. **D-51** still required for visible UI. High-risk: `sonnet-instruct:` + `sonnet-verify:` + `security-gate-id` **PROTECTED**.

## Authority

SSOT mechanics: `model-gates.mdc` (D-65 lane table, D-36/D-45/Ship, D-50). This file is the plain-language collab lock — it must not reintroduce a mandatory four-Grok stack.
