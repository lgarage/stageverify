# stageverify | Current State

> READ THIS FILE FIRST every session. Hot-tier — hard cap ~30 lines.
> **Memory router:** `PROJECT_STATUS/MEMORY.md` — concern → file → when to read.
> **Product authority (on demand):** `PROJECT_STATUS/svscope_simple.md` — scope wins on conflict; load only for scope disputes.

## Snapshot
- **Standing harness:** **D-47/D-69** universal **97 rule** (`confidence-gate.mdc` alwaysApply — classify, cheapest sufficient, honest ≥97% before edit **and** DONE; subagent PASS ≠ parent conf; ≥97% does not waive gates); **D-60/D-38 PROTECTED** — T3/high-risk = **Sonnet pre-review → D-65 builder → Grok adversarial (`high-risk-adversarial:`) → Sonnet final verify → D-38** (fail-closed; CF/rules deploy still need explicit Dan approval); **D-65** Balanced trim LIVE (does not N/A D-60); **D-66** terminal status = exactly one of DONE|BLOCKED|FAILED|PARTIAL (SSOT `done-signal.mdc`) — always last; idle `Nothing needed`; **D-78 MANUAL TEST FOR DAN** required on user-observable product ship/deploy/repair completion reports (before D-66; N/A one-liner when no Dan path); **D-72 Timekeeper** pending→delivered checkpoints (~10/15/20/25/30/35m; stall10 separate) + sticky D-66 re-nudge ~every 5m after force35 while non-terminal — advise-only, no product deploy.
- **MVP: 100.00% — done** — SSOT reconciled 2026-07-16 (`MVP_PATH.md`). §14 E2E prod re-verify **PASS** away-130 (2026-07-17).
- **Staging Map MVP zoom decision:** v0.0.273 View zoom + Canvas W×H are **ACCEPTABLE FOR MVP**. **Warehouse-Scale Staging Map Navigation** (camera zoom/pan for 500–1,000+ objects) is **POST-MVP / FUTURE** — recorded in `docs/roadmap.md` § Warehouse-Scale Staging Map Navigation — **not an MVP blocker**.
- **Partial deploy:** D-59 P2–P7 on `main` (`49924c8b`, v0.0.204). **Lane C C3-D.2 LIVE — awaiting Dan MANUAL** (PR #146 merge `7aa3a4d8` / **D-91** / **v0.0.288** / gh-pages `index-DCXlSwnP.js` tip `90503059`). CFs ACTIVE: `setVendorInvoiceFieldLessonStatus` rev `setvendorinvoicefieldlessonstatus-00001-hun` gen `1786330915747367`; `listVendorInvoiceFieldLessons` rev `listvendorinvoicefieldlessons-00002-kix` gen `1786330983351461`; `evaluateVendorInvoiceFieldLessonCandidates` rev `evaluatevendorinvoicefieldlessoncandidates-00002-mux` gen `1786330983273502`. Prod lifecycle matrix 29/29 PASS; no-parse proof PASS; **no rules/indexes/parser CFs**. Prior **C3-D.1 ACCEPTED**. **C3-C.1 LIVE** (PR #125 / D-81).
- **PR #147 LIVE — D-84–D-90 Slice 1** exception-oriented vendor Delivered qty SoT (merge `8edb97b0` / **v0.0.287**). [high-risk CF + FE]
- **PR #145 LIVE — Approve → fulfillment decision:** merge `1a8ae4e4` / **v0.0.286** / gh-pages `index-BCFUzHaM.js` (Pages tip `834f87ab`); CF `approveVendorInvoiceImport` ACTIVE hash `08a36b96f7b7068554098636f7e30fddfe2227c0` gen `1786328949406674`; `reassignDeliveryStagingLocation` **not** redeployed (prod hash still `9beb3543…` gen `1786321163248307` — extract-only packaging; occupancy semantics already LIVE); **no rules**. **Await Dan MANUAL TEST.** [high-risk CF + FE]
- **PR #97 UX LIVE:** Invoice Review staging → Delivery Details `StagingLocationBanner` → Staging Map `?assignInvoiceImport=` draft Confirm (inline grid retired); package **v0.0.274**; FE-only; Sol visual PASS; `verify:invoice-assign-location` PASS. CF write path LIVE; list/get sanitize gens `1786310744611440` / `1786310802000858`. No rules/CF in this UX ship.
- **D-80 Will-Call staging release LIVE + Dan accepted** — PR #121 merge `1b4c2eea` / **v0.0.278** (FE tip **v0.0.279** `index-DkKBy263.js`); CF `applyVendorReplyClearBackorder` + `approveVendorInvoiceImport` @ hash `2591e35969eb391007c1bd94866736bd9b85ed67`; no rules. [high-risk CF + FE]
- **Awaiting ops:** (1) **Dan MANUAL TEST** vendor post-PIN job-selection cards (**v0.0.290**). (2) **Dan MANUAL TEST** phone polish after technician PIN (**v0.0.289** — KEEP OPEN). (3) **Dan MANUAL TEST** C3-D.2 Invoice Learning lifecycle (**v0.0.288** — KEEP OPEN). (4) **Dan MANUAL TEST** PR #145 Approve fulfillment (**v0.0.286**). (5) **Dan MANUAL RETEST** D-82 staging map release after Complete Pickup (**v0.0.284**). (6) Delivery Details 2×2 yellow VDO + Staged (**v0.0.283**). (7) Change Location (**v0.0.280**). (8) Settings → **Reconnect Gmail**. Durable PDF = away-138 (blocked).
- Shipping: **v0.0.292** — mobile dispatcher load coalesce (one `listDeliveriesOverview` + 4s catalog cache + one `listVendorInvoiceImports`); FE-only; no CF/rules. Rebased onto PR #152. [fast-safe FE]
- Shipping: **v0.0.291** — PR #152 line-item credit/return classification (Dan approved merge + 8 scoped CFs + gh-pages; supersedes PR #150 CORE-16). [high-risk CF + FE]
- Last shipped LIVE: **v0.0.290** — Vendor post-PIN **This job's deliveries** polish (distinct order cards, secondary scanned context, safe-area footer Back); merge `e65719f8` / PR #154; gh-pages `index-DKDQvJ-d.js` (Pages tip `7ae94562`); FE-only; Sol visual PASS; `verify-vendor-job-list` local+prod 17/17 PASS; no CF/rules. [fast-safe UI / mobile D-27]
- Prior LIVE: **v0.0.289** — Technician post-PIN **Pick up today** landing polish (safe-area shell, header Back, elevated job cards, destination chips); merge `bb1624ea` / PR #151; gh-pages `index-GbvD83MA.js` (Pages tip `e52ba74d`); FE-only; Sol visual PASS; `verify-tech-pin-landing` local+prod PASS; no CF/rules. [fast-safe UI / mobile D-27]
- Prior LIVE: **v0.0.285** — Settings **Invoice Learning** read-only C3-D.1 inspection (Sol visual); merge `9ec43a6c` / PR #144 land; gh-pages `index-ClTFdwCi.js`; verify local+prod PASS; FE-only; no CF/rules. [fast-safe UI]
- Prior LIVE: **v0.0.284** — D-82 Complete Pickup occupancy refresh + sticky map occupancy; FE-only. [fast-safe FE]
- Prior LIVE: **v0.0.283** — Delivery Details Vendor Drop-Off selected **yellow**; Staged authoritative; merge `afee98d0` / PR #139; gh-pages `index-D1UqKJUx.js`. [fast-safe UI]
- Prior LIVE: **v0.0.282** — D-82 dispatcher manual Complete Pickup for Assigned/Planned; merge `2b060815` / PR #133; CF `recordPickupEvent` only hash `1b00a373f3e96564425b00a724c41f897c3bffc0`; gh-pages `index-Bd80S9Mv.js`; Dan PARTIAL FAIL — map spot not released (FE occupancy refresh fix in v0.0.284; CF fields already clear). [high-risk CF + FE]
- Prior LIVE: **v0.0.281** — Delivery Details **2×2 Fulfillment/Status** + Dispatcher table simplify; merge `10c1880e` / PR #132; gh-pages `index-RfGot5WX.js`; FE-only; no CF/rules. [fast-safe UI]
- Prior LIVE: **v0.0.280** — Change Location — PR #130 merge `ac24aedb`; CF `reassignDeliveryStagingLocation` rev `reassigndeliverystaginglocation-00001-vog` hash `9beb3543a76c08c8683c39fa9c2af66e00547987`; gh-pages `index-D-ZXZzTG.js`; prod UI 8/8 + CF probe 10/10; no rules; no other CFs. Waiting Dan MANUAL. [high-risk CF + FE]
- Prior LIVE: **C3-C.1** — PR #125 merge `68ddc9b1`; CF+rules; prod probe PASS. [high-risk CF + rules]
- Prior LIVE: **v0.0.279** — PR #127 Will-Call / Pickup pink primary category; merge `1e0e94b9`; gh-pages `index-DkKBy263.js`; FE-only. [fast-safe UI]
- Prior LIVE: **v0.0.277** — PR #126 Delivery Details **STAGING LOCATION NEEDED** card; merge `141b0045`; gh-pages `index-wWobtXgC.js`; FE-only. [fast-safe UI]
- Prior LIVE: **v0.0.276** — PR #122 Staging Map action buttons into map guide header (merge `365ebd10`); gh-pages `index-CyQEITjs.js`; Sol visual PASS; `verify:shop-map-toolbar` local+prod PASS; FE-only; no CF/rules. Warehouse-scale camera nav recorded POST-MVP in `docs/roadmap.md`. [fast-safe UI]
- Prior LIVE: **v0.0.275** — PR #120 Delivery Details Complete Pickup + Status→Picked Up share `isPickupEligible` / `recordPickupEvent` SoT (merge `01b3bb4d`); gh-pages `index-DMbwfhP-.js`; Sol visual PASS; `verify:delivery-drawer-status` local+prod PASS; FE-only; no CF/rules. [fast-safe UI]
- Prior LIVE: **v0.0.274** — PR #119 Invoice Review Assign Location via Staging Map (PR #97 UX); no inline picker; draft Confirm only; FE-only; no CF/rules. [fast-safe UI]
- Prior LIVE: **v0.0.273** — PR #116 Staging Map view zoom + logical canvas size (merge `a68a6c27`); gh-pages `index-BFt4Qudd.js`; Sol visual PASS; `verify:shop-map-zoom` local+prod PASS; FE-only; no CF/rules. [fast-safe UI]
- Prior LIVE: **v0.0.272** — PR #115 Delivery Details **Complete Pickup** + Needs Attention summary (merge `3a37ecb8`); gh-pages `index-DI0q9fwO.js`; Sol visual PASS; FE-only; no CF/rules. [fast-safe UI]
- Prior LIVE: **PR #112 D-79** — fulfillment persist CF `approveVendorInvoiceImport` only; merge `02a48bbb`; CF hash `f27f5aebfb42de5d309f34be967cbf7e012ed362` @ 2026-08-09T21:09:53Z. [high-risk CF]
- Prior LIVE: **v0.0.271** — PR #113 Settings PIN visit-scoped **PIN # updated** row badge (merge `64197430`): FE-only; Sol visual PASS; `verify:settings-technicians` local+prod PASS. Waiting Dan badge UX acceptance. [fast-safe UI]
- Prior LIVE: **v0.0.269** — PR #110 Settings PIN Save tx fix (merge `2038e85f`): CF setAccessPin/upsertManagementPin/setManagementPin + FE toast `New PIN saved`; prod rotate probe PASS. Dan reports PIN save working. [high-risk CF + FE]
- Prior LIVE: **v0.0.268** — PR #103 Settings PIN Save persist + Confirm New PIN + Cancel/Save adjacent (merge `3cb3a804`): FE strip `pinHash`/`pinConfigured`/`pinEncrypted`/`pinCode` before metadata; `setAccessPin` before client vendor/tech update; root cause was stale pin metadata re-merge blocking CF. **No CF/rules deploy.** Non-secret prod smoke PASS; privileged Save = Dan manual. [high-risk auth/session UI]
- Prior LIVE: **v0.0.267** — Staging Map assign-mode auto-exit after successful Assign Location confirm (banner clears, `assignDelivery` cleared, browse restore; X cancel unchanged); merge `804c74c8`; prod verify-assign-mode-exit A–H PASS. FE-only. [fast-safe UI]
- Prior LIVE: **v0.0.266** — Unplanned status single orange badge (no Assigned/Planned double); U1 verify-fixture cleanup + teardown harden (default loc UV); unresolved staging refs → Needs staging / Staging location missing. FE-only. [fast-safe UI]
- Prior LIVE: **v0.0.265** — Dispatcher Deliveries status legend/row wording+colors: Assigned/Planned yellow, Staged green (not purple), Unplanned bright orange, Shop Stock gray; `Awaiting Delivery`→`Assigned / Planned` display label; FE-only. [fast-safe UI]
- Prior LIVE: **v0.0.264** — restore Delivery Details **Assign Location** after Will-Call → Vendor Drop-Off (PR #96 merge `59c9221a`): fulfillment/import-status sync + yellow CTA → Staging Map; `getDeliveryDetails` id fix; prod smoke Will-Call→Vendor Drop-Off→Assign→spot G2→drawer chip PASS (still LIVE under tip **v0.0.265** `index-CebOnuC6.js`). FE-only; no CF/rules. [fast-safe UI]
- Prior LIVE: **v0.0.263** — Dispatcher Delivery Details drawer loads D-73 unplanned shells (no jobId/PO/items): `getDeliveryDetails` no longer throws on missing job; drawer shows Needs job match / — empties; `verify-unplanned-delivery-drawer` 11/11. FE-only (no CF/rules). [fast-safe]
- Prior LIVE: **v0.0.262** — Vendor-run multi-job cards unified with Delivered hub: `vendor-mobile-shell` + hub card chrome, 16px card gaps, Back above Delivered with safe-area footer, completed rows compact green + Undo on expand; bulk deliver + stable order kept; reusable unplanned CTA preserved; `verify-vendor-monday-safe` 63/63. FE-only. [fast-safe UI / mobile D-27]
- Prior LIVE: **v0.0.261** — Vendor Drop-Off **reusable unplanned fallback CTA** (PR #92 merge `3bab991c`): prior listed deliveries no longer hide “Don't see this delivery?” → “Add unplanned delivery”; gh-pages `index-NzFS63ij.js`; `verify:vendor-unplanned-delivery:prod` 7/7 PASS; FE-only (no CF/rules). [fast-safe UI / mobile D-27]
- Prior LIVE: **v0.0.260** — Retire obsolete mobile **Receive Delivery** bare entry (`VendorNativeQrEntry` + manual Delivery ID): bare `#/receive` → location-QR recovery; `#/receive?zone=` / `#/r?z=` → `#/s?loc=`; `#/receive?id=` VendorPinGate kept; hub Vendor Portal / Legacy Check-In removed; gh-pages `index-BaND2UXu.js`; merge `9276b783` / PR #91. FE-only. [fast-safe UI / mobile D-27]
- Prior LIVE: **v0.0.258** — Vendor Drop-Off **delivered-card auto-collapse** (merge `b61096b0` / PR #89): after Mark Delivered, that card collapses in place to compact green DELIVERED row; independent expand/collapse; vendor-run list keeps stable order (no delivered section reorder); Undo on expanded delivered hub; gh-pages `index-DocTBZLl.js`; `verify-vendor-monday-safe` local+prod 58/58. FE-only. [fast-safe UI / mobile D-27]
- Last shipped LIVE: **v0.0.257** — PR #85 **D-75 Invoice Review scoring Phase 1** (merge `800012fd`): verified C2 corrections clear stale `confidence<85` / sticky `humanReviewRequired` vetoes when CURRENT readiness is clean; `confidenceScore` stays parse diagnostic; gh-pages `index-y3aBxUUO.js`; CF eligibility/apply/reparse/inbound redeployed. [high-risk CF + FE]
- Last shipped LIVE: **v0.0.256** — location-scan PIN vertical spacing polish (merge `17717997`): more header↔card gap + top/bottom air; gh-pages `index-TTCy-EDh.js`; FE-only. [fast-safe UI / mobile D-27]
- Last shipped LIVE: **v0.0.255** — PR #83 **D-74 neutral location-scan PIN** (merge `7703b024`): CF `resolveLocationScanPin` + global access-PIN uniqueness LIVE; gh-pages `index-CvZU2kJN.js`; role selector removed; PIN routes vendor/tech/office. [high-risk CF + mobile UI D-27]
- Last shipped LIVE: **PR #81** — Lane C **C2 truth-state consistency** (merge `95897c3b`): CF `reviewAgentTurn` `reviewagentturn-00007-xiv` + `reparseVendorInvoiceImportCallable` `reparsevendorinvoiceimportcallable-00017-xex` + `syncInboundGmail` `syncinboundgmail-00046-jep`; page-1 audit self-heal + chat truth-state prod PASS; no FE/rules. [high-risk CF]
- Last shipped LIVE: **v0.0.253** — PR #79 location/Technician PIN mobile polish (role-track white-alpha; VendorPinGate `embedded` top-flow; short-viewport Verify); gh-pages only (no CF/rules). Merge `f921c7f4`. [fast-safe UI / mobile D-27]
- Last shipped LIVE: **v0.0.259** — PR #86 **D-76 vendor company PIN + uniqueness dual-check** (merge `07b3cd17` / tip `a28e8e54`): prod data `companyWideSessionEnabled=true` on Johnstone/First Supply/Ferguson; Unplanned Verify Vendor fixtures cleaned; CF `setAccessPin` + `migrateAccessPins` + `upsertManagementPin` + `setManagementPin` + `resolveLocationScanPin` redeployed (dual-check `global_` + legacy typed uniqueness). No firestore.rules; no gh-pages (CF + scripts/docs). [high-risk CF]
- Last shipped LIVE: **v0.0.254** — Vendor Drop-Off **unplanned-delivery fallback** (D-73, PR #82 merge `28c1911a`): CF `verifyVendorPin` + `matchUnplannedVendorDelivery` + `createUnplannedVendorDelivery` + `confirmUnplannedVendorDeliveryMatch` LIVE; gh-pages `index-BgRqvF-w.js`; `verify:vendor-unplanned-delivery:prod` PASS. [high-risk CF + mobile UI]
- Shipping: **v0.0.252** — Technician Pickup **visual consistency + FE speed** (PR #78): Sol PIN/job UX alignment with Vendor Drop-Off; progressive job shell + SPA navigate; released-jobs cache; non-blocking PIN session; no CF/`minInstances`. [fast-safe FE / mobile UI]
- Last shipped LIVE: **v0.0.251** — Lane C **C2 live reconcile** (PR #76 merge `ac89540c`): CF `reviewAgentTurn` + `applyInvoiceReviewFieldCorrection` + `reparseVendorInvoiceImportCallable` + gh-pages; apply→Parsed header/warnings/eligibility immediate; Refresh preserves `fieldCorrectionLog`. [high-risk CF + FE]
- Last shipped LIVE: **v0.0.251** tip `ab2c1b2c` (bundle `index-5AwDxAFa.js`) includes prior **v0.0.250** Named Admin (#73 merge `8fc0ca00`) — first-Admin bootstrap verified in production (count=1). [high-risk CF + FE]
- Prior: **v0.0.249** — Vendor Drop-Off **Phase 1 performance** (PR #74): CF `verifyVendorPin` bootstrap + gh-pages PIN UX/hub progressive paint + keypad visual polish; no `minInstances`. Merge `f8d86184`. [high-risk CF + fast-safe FE]
- Prior: **v0.0.248** — Lane C **C2** current-import field correction: CF `reviewAgentTurn` + `applyInvoiceReviewFieldCorrection` + `firestore.rules` (`vendorInvoiceFieldCorrections`) + gh-pages; propose→confirm→apply→audit→Parsed Import refresh; allowlist PO/order#/invoice#; no C3/C4. Merge tip `c0375110`. Before C3: amend D-59 #7/#8 for field learning. [high-risk CF/rules + FE]
- Prior: **PR #66 on main** — fulfillment/staging table+drawer UX + dark contrast (merge `05670dc8`); gh-pages not yet from this tip. [fast-safe UI]
- Prior: **v0.0.244** — merge PR #57 Vendor `companyName`/`locationName` + Company — Location display helper + larger Vendor Communications workspace (optional delivery, email auto-populate); **gh-pages only** (no CF/rules/migration). Merge `ab21e246`. [fast-safe UI deploy]
- Prior: **PR #64 on main** — invoice Parsed Import **staging-on-Approve** + **D-67 matched-delivery** (merge `7161e271`); CF deploy status separate if not already live. [high-risk CF + fast-safe UI]
- Prior: **v0.0.244** — inactive Manager/Dispatcher **Remove** (`removeDispatcher` CF + Settings UI); smoke-mgr-pr50 + smoke-pr50 Auth accounts permanently removed from production roster; protected ops accounts retained. Merge PR #65 `7b93bb0d`. [high-risk CF + UI]
- Prior: **v0.0.244** — inactive Manager/Dispatcher **Remove** (`removeDispatcher` CF + Settings UI); smoke-mgr-pr50 + smoke-pr50 Auth accounts permanently removed from production roster; protected ops accounts retained. Merge PR #65 `7b93bb0d`. [high-risk CF + UI]
- Prior: **v0.0.243** — portal sidebar full `stageverify-logo-main.svg` wordmark in white frame via shared `PortalSidebar` / `StageVerifyBrandMark` (`variant="wordmark"`); no duplicate STAGEVERIFY text; Dispatcher Portal sublabel kept; print chrome unchanged. Merge PR #63; gh-pages only (no CF/rules). [fast-safe UI deploy]
- Prior: **v0.0.242** — dispatcher portal sidebar used authoritative `stageverify-app-icon.svg` via `StageVerifyBrandMark`; gh-pages only (no CF/rules). [fast-safe UI deploy]
- Prior: **v0.0.241** — PR #59 **4–6 digit access PIN** + coordinated CF deploy (`setAccessPin`, `setManagementPin`, `upsertManagementPin`, `verifyTechnicianPin`, `verifyVendorPin`, `verifyManagementPin`, `migrateAccessPins`) + gh-pages. Existing 4-digit PINs still work. [high-risk CF + fast-safe UI]
- Prior: **v0.0.240** — Settings **row-scoped Admin Access** gh-pages only (PR #56 UI); PR #58 dual-read CF already live. [fast-safe UI deploy]
- Prior: **v0.0.239** — sticky manual invoice reject LIVE (CF + gh-pages): CF blocks `reopen` unless `isSystemAutoRejectedImport`; UI hides Re-open for manual rejects; harness never clicks Re-open. Prod sticky verify PASS (6168733 + 3316448A stay rejected; callable reopen blocked). [high-risk CF + fast-safe UI/harness]
- Prior: **v0.0.238** — dark-mode navy layered surfaces (L0–L3); merge PR #53 `4b406c13`. [fast-safe UI]
- Prior: **PR #55** Gmail auto-archive after durable ingest (D-64) — INBOX remove only after `parsed`/`reply_processed`; soft-fail; View Original PDF still Gmail fetch. Merge `9b908abd`. [high-risk CF]
- Prior: **v0.0.237** — merge PR #54 Monday-MVP dispatcher usability: staging Location column parse/`locById` (`id: docId`); global text selection restored; drawer **Will-Call / Pickup from Vendor**; list empty/will-call staging **—**; gh-pages only (no CF/rules). Merge `2f3b4ec8`. [fast-safe UI]
- Prior: **v0.0.236** — merge PR #51 dispatcher Deliveries final column order (Status → Fulfillment → Vendor → Job Name → Invoice # → PO # → Staging Location → Items → Delivery / Pickup Date → Issue → Assigned Technician → Action); Job #/Order # table-only remove; Status workflow-only; Issue exceptions-only; gh-pages only (no CF/rules). Merge `b183b4e6`. [fast-safe UI]
- Prior: **v0.0.235** — merge PR #50 Settings **PIN & Access Management** unified roster (Manager/Dispatcher Auth + Technician/Vendor/Management PIN); separate Dispatcher accounts section removed; existing Auth/PIN stores unchanged; gh-pages only (no CF/rules). Prod Manager verify PASS (`verify:settings-technicians:prod` + `verify:settings-management-pins:prod` + `verify:settings-dispatchers:prod` + `verify:admin-appearance` @ prod). Merge `f9278fcb`. [fast-safe UI]
- Prior: **v0.0.234** — dispatcher section-separation: `--admin-section-*` tokens + `.admin-section` wrappers (Needs Review email, Invoice Imports, Search+Filter, Deliveries); nested card/table chrome cleared; dark/light text + `--admin-on-navy` strong-fill fixes preserved; gh-pages only (no CF/rules). [fast-safe UI]
- Prior: **v0.0.233** — merge PR #49 drawer fulfillment/staging UX: Vendor Drop-Off / Will-Call / Pickup wording; STAGING LOCATION NEEDED only when required∧unassigned, directly under Fulfillment; gh-pages LIVE; `verify:delivery-drawer-status` (+ :prod) PASS. [fast-safe UI] Merge `068e09c6`.
- Prior: **v0.0.232** — merge PR #46 technician assignment UX: Settings release checklist removed; drawer-only ASSIGNED TECHNICIAN / exclusive single-tech assign; gh-pages LIVE; `verify:settings-technicians:prod` + `verify:dispatcher-job-release:prod` + `verify:admin-appearance` PASS. [fast-safe UI] Evidence: solution-verifier `bc-2209562f`+`bc-2224a578` PASS; ui-before-after settings+dispatcher-job-release; ui-playwright-verifier `bc-ef7d0b5e`+`bc-01ccc03b` PASS; build-checker `bc-76409955`+`bc-a672883e` PASS; ship merge `6ff88741`.
- Prior: **v0.0.231** — dark-mode readability hierarchy (`--admin-text-data` / label / secondary / table-header); Vendor Communications + credit/return + invoice/settings leftovers tokenized; light mode + `stageverify-theme` persistence unchanged; `verify:admin-appearance` + `verify:invoice-review` + `verify:dispatcher-nav` PASS. [fast-safe UI]
- Prior: **v0.0.230** — admin/dispatcher UI modernization (shared `.portal-shell` radius/spacing/control/shadow tokens + utilities; cards/tables/toolbars/drawers/modals); Light/Dark persistence unchanged; vendor/technician mobile unchanged; `verify:admin-appearance` + `verify:dispatcher-nav` PASS. [fast-safe UI]
- Prior: **v0.0.229** — merge PR #45: Settings **PIN & Access Management** (tech + company-vendor + management roster; job PINs excluded); Light/Dark theme preserved; `verify:settings-technicians` + `verify:settings-management-pins` + `verify:admin-appearance` PASS. [fast-safe UI]
- Prior: **v0.0.228** — dispatcher/admin Light/Dark appearance (`stageverify-theme` localStorage; FOUC bootstrap; floating toggle); vendor/technician mobile themes unchanged; `verify:admin-appearance` PASS. [fast-safe UI]
- Prior: **v0.0.227** — merge PR #44: vendor hub Location code header + Invoice # row; `verify:vendor-monday-safe` location/invoice/accordion/CTA asserts PASS. Live Settings still `full_checkin` until ops flips exception_only. [fast-safe UI]
- Prior: **v0.0.226** — merge PR #43: vendor hub tap-to-expand expected items (read-only accordion); `verify:vendor-monday-safe` accordion asserts PASS. [fast-safe UI]
- Prior: **v0.0.225** — merge PR #41: Monday-safe vendor hub — no-spot disables Mark Delivered (“Ask dispatch for a staging spot.”), “Report a Problem” wording; `verify:vendor-monday-safe` PASS. [fast-safe UI]
- Prior: **v0.0.224** — merge PR #40: CF `recordTechnicianJobOpen` + soft-fail client TECH_JOB_OPENED evidence; CF **deployed** on stageverify-db; gh-pages LIVE. [high-risk CF + fast-safe client]
- Prior: **v0.0.223** — merge PR #39: Settings Workflow **StageVerify Start Date** (`appSettings.stageVerifyActivatedAt`, YYYY-MM-DD); Green Bay live `2026-08-10` (editable); `verify:settings-staging:prod` PASS. [fast-safe UI]
- Prior: **v0.0.222** — merge PR #38: unauthenticated technician Complete Pickup (remove client getDoc before recordPickupEvent). [fast-safe]
- Prior: **v0.0.218** — invoice reject dialog requires non-empty "Why was this rejected?" note for all reasons; shared `InvoiceRejectReasonDialog` deduped from inspect modal; `verify:invoice-review` + `verify:invoice-reject-reason` PASS. [fast-safe UI]
- Prior: **v0.0.217** — credit/return defense-in-depth: ingest auto-rejects structural credits to Rejected Invoices; CF blocks approve/create_shell/relink; auto-import blocked; default Deliveries board hides `creditReturnLinked`; `test:credit-return-delivery-block` + `test:approve-vendor-invoice-import` PASS. [high-risk CF + fast-safe client]
- Prior: **v0.0.216** @ `b9360d5` — delivery drawer STATUS dropdown **Reject…** action; credit/return banner kept; `verify:delivery-drawer-status` PASS. [fast-safe UI]
- Prior: **v0.0.215** @ `eb000e7` — credit/return delivery banner + list badge; drawer Reject linked import (approved credit slip-throughs) via `approveVendorInvoiceImport` + training lesson; `verify:delivery-drawer-status` PASS local+prod. [high-risk CF + fast-safe UI]
- Prior: **v0.0.214** @ `ee696f3` — systemic will-call pickup preserve. [high-risk CF]
- Prior: **v0.0.211** — delivery drawer closes after successful STATUS workflow mutations (Confirm Pickup, revert, mark shipped, spot+ready_for_pickup, dropdown status); stays open while pickup/spot forms pending or on Cancel; `verify:delivery-drawer-status` PASS. [fast-safe UI]
- Prior: **v0.0.209** (`c4d3f1f`) — delivery drawer: `pendingStatusSelection` keeps STATUS label + dropdown on Picked Up while pickup form pending; pickup CF errors surfaced in form; `verify:delivery-drawer-status` PASS local+prod. [fast-safe UI]
- Prior: **v0.0.208** — will-call Confirm Pickup CF (`recordPickupEvent` skipsShopStaging path + `closed_picked_up`); client `closed_picked_up` → Picked Up chip + Complete board; `test:pickup-authority` + `test:invoice-shell-display` PASS; CF deployed (`4755802`); gh-pages LIVE. [high-risk CF + client display]
- Prior: **v0.0.207** — delivery drawer status dropdown + drop-off/will-call fulfillment toggle under PO# (replaces Advanced Manual Controls); labels Ready for Pickup / Picked Up; `verify:delivery-drawer-status` PASS; D-62 post-D-50 retry rounds harness (`model-gates.mdc`, `DECISIONS.md`). [fast-safe UI + firestoreService client writes]
- Prior: **v0.0.206** — D-60 dispatcher account provisioning: Settings manager panel (`listDispatchers`/`provisionDispatcher`/`deactivateDispatcher` CFs), `/no-access` page, `ProtectedRoute` gates `dispatcherRoles`; `verify:login` PASS; `verify:no-access` + `verify:settings-dispatchers` need env creds/manager grant.
- Prior: **v0.0.205** — D-60 auth-native forgot-password on dispatcher login (`verify:login` PASS).
- Prior: **v0.0.197** — Dispatcher Unassign (drawer + table ×); will-call shells on default Deliveries board.
- **D-59 P1 deploy:** gh-pages **built** @ `74414db` · https://lgarage.github.io/stageverify · **CF** (`proposeVendorIgnoreRule` + updated functions on `stageverify-db`) **deployed**.
- Active Phase: Location-first Phase 6 Slice C (C1 shipped) — Slice B audit walk next.
- Stack: React 19 + TS, Vite 8, Firebase 11.x — https://lgarage.github.io/stageverify · `stageverify-db`

## Active Blockers
1. **Shelving decision** — layout IDs provisional (default shop layout locked for v1 map).
2. **GCP Pub/Sub push path** — optional; poll/Refresh Now proven.

## Immediate Next Step
- **Phone check after v0.0.290:** hard-refresh → location QR → vendor PIN → **This job's deliveries**: distinct order cards (PO / staging chips / Not assigned yet), scanned context secondary, footer Back above Safari chrome; tap card → same hub as before. KEEP until Dan accepts.
- **Phone check after v0.0.289:** hard-refresh → location QR → technician PIN → **Pick up today** landing: notch-safe header Back, tappable job card with GO TO chip, no Back under Safari chrome; tap card → pickup. KEEP until Dan accepts demo polish.
- **C3-D.2 Manager lifecycle LIVE — await Dan MANUAL** — PR #146 merge `7aa3a4d8` / **v0.0.288** / D-91. CFs ACTIVE (set/list/evaluate only). Prod lifecycle A–P + no-parse PASS. Settings → Invoice Learning Activate/Reject/Suspend/Reactivate. Active = approved for future parser application (no parse effect until C3-E). KEEP job open until Dan accepts. Next after accept: **C3-D.3** roadmap-only.
- **PR #145 Approve fulfillment LIVE (v0.0.286)** — hard-refresh → Invoice Review → safe pending → Approve (choice, no write) → Cancel; Drop-Off → map → Cancel; Drop-Off → Confirm; separate Will-Call → Confirm; no footer Assign Location; hard-refresh persistence. KEEP open until Dan accepts.
- **Delivery Details 2×2 yellow + Staged authoritative (v0.0.283)** — hard-refresh → Vendor Drop-Off yellow; no Current/Reject/inline picker; Staged with G10 transitions without dropdown; no staging → STAGING LOCATION NEEDED; Invoice Review Reject intact; D-82 Complete Pickup warning preserved. KEEP Cursor job open until Dan accepts.
- **D-82 manual Complete Pickup LIVE (v0.0.282)** — hard-refresh → Assigned/Planned Vendor Drop-Off → **Complete Pickup** → not-ready warning + outstanding/blocker summary → Cancel (no write) → Confirm with technician → Picked Up + staging released + items evidence intact + hard refresh. Keep Cursor job open until Dan accepts.
- **Change Location LIVE (v0.0.280)** — hard-refresh → Vendor Drop-Off with staging → yellow **Change Location** → map reassign → Cancel (no write) → Confirm new spot → drawer/list/map + hard refresh; Will-Call no CTA; Assign merge unchanged.
- **Will-Call pink category (v0.0.279 LIVE)** — regression reference: Drop-Off+G12 → Will-Call → pink category, no green Staged; reverse → STAGING LOCATION NEEDED.
- **Delivery Details staging-needed card (v0.0.277)** — hard-refresh dispatcher → open Vendor Drop-Off with no staging → confirm STAGING LOCATION NEEDED + yellow Assign Location; Assign → Staging Map confirm; Will-Call hides card.
- **PR #97 Invoice Review Staging Map Assign LIVE (merge `b62401eb` / PR #119 / packaged v0.0.274; served under tip **v0.0.276**) — waiting Dan manual acceptance:** Will-Call → Confirm Drop-Off → STAGING LOCATION NEEDED card → Assign Location → Staging Map → Confirm draft → reopen shows location + Change Location; hard refresh; spot not occupied pre-Approve.
- **Staging Map zoom/canvas (v0.0.273)** — hard-refresh Staging Map → Edit Locations → zoom −/+ / 100% / Fit; enlarge Canvas Wider/Taller; drag a spot while zoomed; Done editing → refresh → confirm logical positions unchanged.
- **C3-B Johnstone parser LIVE** — PR #111 merge `04031ca9`; CF `syncInboundGmail` + `reparseVendorInvoiceImportCallable` deployed (C3-B tip hashes then PR #97 redeploy live `ce70ab9f` / `6b5a443e`; srcGen `1786309783217144` / `1786309783253545`). Prod reparse `vii-19fe78ea427b3142-page-0` → PO `2205 EARLY` (not `…PICKUP`). Next **C3-C**. No firestore.rules.
- **Settings PIN # updated badge LIVE (v0.0.271 / `index-DZiw9ihY.js` / merge `64197430`) — waiting Dan manual acceptance:** hard-refresh → Settings → PIN & Access → successful PIN save → toast `New PIN saved` + green **PIN # updated** right of Deactivate; stays for visit; clears when leaving Settings. Keep Cursor job open until Dan reports pass/fail.
- **Assign Location restore LIVE** (PR #96 `59c9221a` / packaged v0.0.264; currently served inside tip **v0.0.265**): hard-refresh dispatcher → Will-Call → Vendor Drop-Off → yellow Assign Location → Staging Map assign → confirm spot → drawer shows location.
- **Dispatcher check after v0.0.263:** hard-refresh; open First Supply unplanned row (invoice/ref 12345679) — Delivery Details must load (Unplanned + Needs job match + staging), not “Unable to load delivery details.”
- **Phone check after v0.0.262:** hard-refresh; Johnstone company PIN → Your deliveries: clear card gaps, Back above Delivered (not jammed under browser chrome), completed cards compact green + expand Undo; undelivered stay expanded; Don't see this delivery CTA still available with prior rows.
- **Phone check after v0.0.261:** hard-refresh; Ferguson location QR → PIN with a prior unplanned still shows **Don't see this delivery? → Add unplanned delivery**; second different reference creates a separate Unplanned record.
- **Phone check after v0.0.260:** hard-refresh; bare `#/receive` must show “Scan a location QR” (not Receive Delivery / Delivery ID); location QR → Enter PIN unchanged; delivery deep links `#/receive?id=` still PIN→hub.
- **D-76 vendor company PIN LIVE** — PR #86 merge `07b3cd17` / tip `a28e8e54` / package **v0.0.259**; CF uniqueness dual-check LIVE. Phone check: First Supply / Ferguson on `#/s?loc=` should resolve (companyWide data already fixed). Ops later: rotate Johnstone↔job-1 and job-2↔tech-verify colliding PINs (not silently changed).
- **Invoice Review scoring Phase 1 LIVE (D-75)** — PR #85 merge `800012fd` / **v0.0.257** / `index-y3aBxUUO.js`; CF apply/reparse/inbound/reviewAgentTurn redeployed. Prod: `verify:invoice-review:prod` PASS; eligibility fixtures 14/14; C2 apply+PO survive in chat verify PASS. Adjacent `history-on-reopen` FAIL was **test-only** (first-paint race before mock subscribe hydrates; sessionStorage + live Firestore intact) — harness fix PR #90 merge `53551d6d` (no product deploy). **C3-A COMPLETE**; **C3-B LIVE** (PR #111 / merge `04031ca9`; CF syncInboundGmail + reparseVendorInvoiceImportCallable) — next **C3-C** examples store (no parse effect).
- **Neutral location-scan PIN LIVE (D-74)** — PR #83 merge `7703b024` / **v0.0.255** / `index-CvZU2kJN.js`; CF `resolveLocationScanPin` + uniqueness write path deployed. Hard-refresh phone and confirm bundle.
- **Lane C C2 truth-state LIVE** — PR #81 merge `95897c3b`; CF revisions above; prod page-1 self-heal + page-2 isolation PASS. **C3-B LIVE** — then **C3-C** only (no C3-D/E/F leap). Dan/ops: Settings → **Reconnect Gmail**, then smoke one invoice through ingest → Inbox archive + View Original PDF.
- **Technician Pickup FE consistency+speed (PR #78)** — already LIVE as **v0.0.252**. Remaining bottleneck: CF `verifyTechnicianPin` + sequential `getTechnicianReleasedJobs` N+1 (high-risk CF — needs Dan approve; do **not** enable `minInstances` yet).
- **Named Admin (#73) LIVE + production authorization closeout SAFE** — tip `5d176b03` / package **v0.0.251** / gh-pages bundle `index-5AwDxAFa.js` (includes named-Admin from v0.0.250 merge `8fc0ca00`). Prod re-verify 2026-08-09: active Admin count=1; `accessControlLocks/firstAdmin` claimed; Admin secret hash-only/`revealable:false` (no recoverable PIN); second bootstrap denied; Manager cannot start/reveal Admin Access or alter/demote/deactivate Admin; last-active protection intact via sole-Admin + Manager deactivate denial; Manager Settings + login prod PASS. Admin Access correct-PIN path manually verified by Dan (PIN not re-logged / not in agent env). No further deploy.
- **Vendor Drop-Off Phase 1 LIVE** — PR #74 merge `f8d86184`; CF `verifyVendorPin` + gh-pages **v0.0.249** (`index-CM9rIl0p.js`). Prod warm PIN→useful hub **median 604 ms** (samples 565–662; target ≤1500); session reuse **~500–560 ms**; details CF off useful critical path (~110–200 ms after hub). Do **not** enable `minInstances` yet (cold scale-up still candidate for later).
## Queued product (deferred)
- **away-138** — durable StageVerify PDF storage (D-64 TARGET); blocked until Dan queues post–Monday MVP.
- **After D-59 phases P1–P7 deploy:** **away-137** — tighten `firestore.rules` so `deliveries`/`items` are not writable by any authenticated client; high-risk; blocked until training-note hardening phases complete (`docs/training-note-ignore-spec.md` §29 #9).
- **Lane C C3:** **C3-A COMPLETE**; **C3-B LIVE** (PR #111 `04031ca9`). **C3-C.1 LIVE** (PR #125 `68ddc9b1`, D-81). **C3-D.1 ACCEPTED** (PR #137 `7a6d5bb3`, D-83; prod 23/23 PASS; read-only Invoice Learning UI accepted). **C3-D.2 shipping** PR #146 / **D-91** — `setVendorInvoiceFieldLessonStatus` + Settings lifecycle UI; Dan deploy approved. **C3-D.3 Novel Format Trainer** planned NEXT (investigation-only). C3-C.2 list/UI deferred. Authorized fields only: `customerPoOrReference`, `vendorOrderNumber`, `vendorInvoiceNumber`. P8 `fulfillment_mapping` stays separate.
- **Phase 5 Slice B:** pickup verification v2 polish (per-location confirms, exception flags).

## Canonical references
- **Decisions:** `PROJECT_STATUS/DECISIONS.md` (+ `DECISIONS_ARCHIVE.md` when superseded)
- Handoff: `PROJECT_STATUS/archives/MINI_LIBRARIAN_HANDOFF.md` | Queue: `away-list.json` + `NEXT.md` | Validate: `npm run away:validate`

## Update Protocol
- Ship: `npm run away:ship` → `estimate-log.md` → `npm run away:validate` (auto-syncs CURRENT_STATE + Phase Tracker + roadmap from verify PASS) → commit.
