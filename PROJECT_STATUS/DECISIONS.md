# Decision Registry

## How this file works
- Active section capped at ~40 decision lines; one line per decision.
- Format: `D-NN (YYYY-MM-DD) [harness|product]: <decision> — because <compressed why>.`
- Stable IDs — never renumber. Superseded → `PROJECT_STATUS/DECISIONS_ARCHIVE.md` with `superseded-by D-NN`; never delete.
- `[harness]` = agent/orchestration; `[product]` = StageVerify behavior/architecture.
- Record new decisions in the **same commit** as the work that implements them.

## Active decisions

D-01 (2026-07-09) [harness]: Two-tier ship model — fast-safe (`src/`, `public/`, verify scripts, docs, PROJECT_STATUS, routine service logic) build/commit/push/deploy without approval; high-risk (auth, `firestore.rules`, CF write paths, secrets, billing, data deletion, schema migrations, deploy config, auth/route-guard files under `src/`, root `package.json` deploy wiring) require explicit Dan approval before implementation **and** deploy — because bad backend deploys are not cheaply revertible like a bundle; ambiguous tier → classify high-risk and ask.
D-02 (2026-07-09) [harness]: Tiered verification ladder — Grok 4.5 Fast = post-ship ship-verifier (scope/correctness) and pre-decision critical reviewer (plans/architecture); Sonnet 4.6 = mandatory security gate (unchanged); Fable 5 = work verifier for tier-3/phase-boundary work only — because each check should use the cheapest capable model.
D-03 (2026-07-09) [harness]: Evidence standard — every review/verification claim requires subagent Task ID + model line in the completion report; missing evidence = NOT RUN — because reviews were previously claimed but never executed.
D-04 (2026-07-09) [harness]: Universal fix-closure — whichever model reported an issue re-verifies the fix before closure — because fixes were being closed without the reporter confirming them.
D-05 (2026-07-09) [harness]: Lessons compression — lessons files keep only compressed reusable rules that change future behavior; specific incidents get distilled then archived — because per-incident logs are context bloat.
D-06 (2026-07-09) [harness]: Trim always-applied rules in place; defer flipping time-awareness / parallel-agent-strategy to on-demand until attachment reliability is proven — because heuristic attachment risks silent behavioral regression (Grok review finding).
D-07 (2026-07-09) [harness]: Away/overnight workflow follows the same ladder — per-item ship verification, queue-time high-risk `danApproved` schema check, mandatory verifier report lines — because away batches must not bypass harness gates.
D-08 (2026-07-09) [harness]: Decision registry + handoff protocol — decisions recorded same-commit; "prepare for new conv" flushes in-flight state; new conversations bootstrap from `CURRENT_STATE.md` + this file — because handoff prompts should never need to be hand-written.
D-09 amended (2026-07-20): Vendor PIN remains job-scoped by default (D14). Per-vendor `companyWideSessionEnabled` (dispatcher-set, default false) unlocks a separate company-PIN path: after any location QR, the vendor company PIN creates a vendor-scoped session listing that vendor's open orders across jobs; job PINs never elevate and stay job-scoped. Each checklist row is two lines (job name; spot · invoice · PO); rows without an assigned/planned spot cannot be checked or bulk-delivered (client + server). Bulk Delivered requires a confirm dialog listing distinct jobs; overflow suggestions stay empty-unassigned-only; auto-confirm timeout deferred to a separately approved ship. StageVerify makes each job and assigned spot unmistakable and surfaces misses for coaching; wrong-bay accountability remains with the vendor to train drivers.
D-10 (2026-07-09) [product]: Printed QR strategy is static permanent location signs (`#/s?loc={code}`); occupancy-dynamic QR-flip rejected; `qr-routing`, `zone-lookup`, `encode-qr` dossier § refreshed — legacy dispatcher zone e-tags still emit `#/receive?` / `#/pickup?` until Phase 3 role-aware resolver ships — because location-first signs must never change when occupancy changes.
D-11 (2026-07-09) [product]: Canonical vendor UI is `ReceivingPage` at `/#/receive` only; legacy `/#/` and `/#/checkin/:id` (plus compact `#/r?`) redirect there — because duplicated vendor UIs caused scan/routing drift.
D-12 (2026-07-09) [harness]: Fable 5 never implements — all file changes go to Composer 2.5; Fable verifies the result and gates ship (ship only after Fable confirms correct, with fixes looping back to Composer per D-04) — because implementation on the expensive model wastes cost and blurs the verify role.
D-13 (2026-07-09) [harness]: Handoff bootstrap is trust-based — new conversations read CURRENT_STATE.md + DECISIONS.md only; no transcript mining, no re-verifying recorded verdicts (handoff flush must record verdict + commit hash per item) — because re-deriving verified state from primary sources burned 37% of a session's tokens and 7 minutes.
D-14 (2026-07-09) [harness]: Autonomy-evidence pairing — no new autonomy without evidence produced for a non-author; evidence means report lines/logs/verdicts, not infrastructure (dashboards must justify separately) — because authority without independent observation is how claimed-but-never-run reviews happened.
D-15 (2026-07-09) [harness]: Organizational concepts must delete existing complexity in the same commit or be rejected; capability additions instead face the 80/20 burden-of-proof (rejected when rare-case value < permanent complexity tax; catastrophic-asymmetry gates like the security gate are exempt) and must name a maintenance owner — because concepts that delete nothing become permanent layers.
D-16 (2026-07-09) [harness]: Harness V1 Freeze — asymmetric (deletions always legal; additions need a named dated pain event ≥2×, a security/production incident, a measured bottleneck, or a real customer gap); charter: PROJECT_STATUS/HARNESS_V1_FREEZE.md — because further harness work now delays StageVerify MVP (CTO decision, adversarially reviewed rounds 1-5).
D-17 (2026-07-09) [harness]: Fable 5 role is CTO for StageVerify delivery — burden of proof flipped: any harness proposal must justify delaying product; Fable actively rejects speculative harness/architecture/automation work per the V1 Freeze (D-16) and keeps effort on MVP — because Dan is naturally attracted to improving systems and delegated the guard role.
D-18 (2026-07-10) [harness]: Auto-gotcha learning — Phase 0–1 extend `classifyVerifyFailure` + manual `gotcha-map` entry + demo regressions; pending→indexer on `away:ship` unchanged; Phase 2 auto `--apply-gotcha` and broadened packet injection require explicit Dan approval — because verify auto-capture logged failures but misclassified spawnSync patch hangs (missing `process.exit`) and never promoted to session-start memory (Grok verify `0c002bcb`, plan `537f0ed9`).
D-19 (2026-07-10) [harness]: Consecutive same-failure Grok stall-advisor — 2nd same fingerprint on same task dispatches one read-only Grok (`grok-4.5-fast-xhigh`, `generalPurpose`, `readonly`) for hypotheses/next experiments; Sonnet diagnose-only when still stuck (different fingerprint on 2nd fail, or 3rd+ fail after Grok) — because repeated identical verify/build failures wasted cycles without a cheap pivot (Dan 2026-07-10 consecutive same-failure stall; D-16 pain logged).
D-20 (2026-07-11, amended) [harness]: **Platform parity** — `.cursor/rules/*` alwaysApply rules bind all Composer sessions (desktop, mobile Cursor, cloud VM) with identical harness: ship-loop tiers, repair loop, planning verify loop, verification ladder, security gate, stall-advisor, verifiers, evidence lines, fix-closure; no mobile-lite orchestration; exceptions only where physically impossible (secrets UI, branch-only ship) documented in AGENTS.md + cloud setup — repair loop detail: on Dan repair intent route to correct verifier (Grok default; Sonnet security/auth/rules/CF; Fable spec-phase; D-19 unchanged); verify → Composer fix → same verifier re-verify until PASS (max 3 cycles); evidence `repair-verifier:` + `fix-verified:`; no self-close — because Composer was self-closing repairs and mobile was treated as lite harness (Dan 2026-07-11; D-16 pain logged).
D-21 (2026-07-11) [harness]: **Planning verify loop** — on planning triggers ("what's next", "what else can be worked on", "what can we work on", roadmap/priority, ranked options, away planning) scouts synthesize → Grok Planning Verifier (`grok-4.5-fast-xhigh`, `generalPurpose`, readonly) cross-checks authoritative sources → PARTIAL/FAIL → Composer revises → same Grok re-verifies until PASS (max 3 cycles); only Grok-PASS answer presented; evidence `planning-verifier:` + `fix-verified:`; platform parity with D-20 — because Composer first-draft planning answers omitted recent ships, invented away IDs, and contradicted CURRENT_STATE (Dan 2026-07-11; D-16 pain logged).
D-22 (2026-07-11) [harness]: **Q&A verify loop** — on non-trivial Q&A (how/why/where/explain/recommend/clarify) on all clients (desktop Windows PC, mobile Cursor, cloud VM) Composer drafts → Grok Q&A Verifier (`grok-4.5-fast-xhigh`, `generalPurpose`, readonly) cross-checks cited sources → PARTIAL/FAIL → Composer revises → same Grok re-verifies (max 2 cycles); evidence `qa-verifier:` + `fix-verified:`; skip trivial single-fact and planning triggers (D-21); platform parity with D-20 — because Dan wants Grok conferral on Q&A answers for accuracy, not only planning (status wording confusion 2026-07-11; Dan explicit).
D-23 (2026-07-11) [harness]: **Doc drift validate + hot-tier auto-sync** — `verify:location-phaseN` prod PASS (Snapshot/Verify) or ship note → auto-write CURRENT_STATE gate closure + Phase Tracker + roadmap; `away:validate`/`away:ship`/`away:sync --write`; validate fails only if auto-sync cannot fix; Dan never hand-edits phase close; **platform parity D-20** (desktop Windows PC + mobile Cursor + cloud VM identical `node` scripts) — because planning agents read stale docs (2× pain; D-16).
D-24 (2026-07-12) [product]: **MVP path SSOT** — `PROJECT_STATUS/MVP_PATH.md` records 80% completion (Fable+Grok confer 2026-07-12), five exit criteria, and fastest-path priority order; all future planning/progress sessions must analyze the full MVP landscape (§14 + roadmap traceability + blockers), not queue head alone, and present the fastest clear path to MVP done — because queue-empty sessions were drifting to harness/phase work while §14 E2E and live email gates remained open.
D-25 (2026-07-12) [harness]: **MVP completion % reporting** — all agents on all clients report `MVP completion: XX.XX%` (2 decimals) in substantive work replies when task traces to `MVP_PATH.md`; update SSOT on milestone ship; skip dedicated line for pure planning (planning still leads with %); rule `.cursor/rules/mvp-completion-report.mdc` alwaysApply; repo commit = PC rule sync — because Dan needs continuous MVP progress visibility without re-asking.
D-26 (2026-07-12) [product]: **E-tags / ESL excluded from MVP scope** — e-tags, Minew ESL, §14 step 26, and all ESL automation are **not** in MVP %, exit criteria, fastest path, or active blockers; post-MVP product track only (`ESL_INTEGRATION_PLAN.md`); revisit after MVP operational with requirements restated + verify/tests — because Dan directed zero e-tag work toward MVP done.
D-27 (2026-07-12) [harness]: **Mobile UI merge+deploy** — when Dan asks for a mobile UI change (vendor check-in, pickup portal, phone-first public routes), agents **always** merge to `main` and `npm run deploy` after build + Playwright pass — no separate “merge/deploy” prompt; overrides cloud branch-only default for that class; high-risk/verify-fail gates unchanged — because Dan validates on iPhone and branch-only ships blocked phone testing (Dan 2026-07-12).
D-28 (2026-07-12) [harness]: **Mechanical evidence gates + verifier calibration log** — CI gate-check (`.github/workflows/gate-check.yml` + `scripts/gate-check.mjs`) fails PRs whose diffs touch high-risk paths (fail-up: all `functions/` except `lib/`, firestore.rules, workflows, firebase.json, src auth-heuristic paths, root package.json scripts/firebase-major) without `security-gate-id` + Sonnet model line (presence-only — RC-3: not proof of invocation; forks can forge); PR/CI-only — desktop direct-to-main stays honor-system (superseded by D-29); verifier verdicts logged to `PROJECT_STATUS/verifier-log.jsonl` via `verifier:log`, `verifier:stats` = admissible D-16 evidence — because Dan directed freeze reopen (owner authority 2026-07-12) to mechanize prose gates and generate the measurements future freeze decisions need.
D-29 (2026-07-12) [harness]: **Ecosystem-wide evidence gate** — extends D-28 to all clients (same Dan reopen authority 2026-07-12): local pre-push hook (`scripts/install-git-hooks.mjs`, auto-installed via npm `prepare` on every machine) blocks direct high-risk pushes to `main` without the security-gate evidence block in commit messages (`--evidence-from-commits`); zeros-base unified on `origin/main`; `GATE_SKIP=1` / `--no-verify` bypass documented (advisory-vs-malice per D-28); push-on-main CI trigger deferred until merge-commit PR-lookup exists (future addition, own D-16 evidence) — because desktop direct-to-main was the honor-system gap after D-28.
D-30 (2026-07-12) [harness]: **Peer deliberation** — verifier findings are proposals, not orders (verifier ≠ boss); author evaluates each finding, adopts or rebuts with reasoning; both agents reach genuine agreement (or present both positions to Dan) BEFORE the cheap implementer applies changes; operational closure per finding = adopted | rebutted (verifier agreed) | escalated; security HIGH rebuttal allowed but unresolved HIGH still blocks push; disposition line mandatory when findings ≥ 1; fix-closure (D-04) unchanged — because Dan observed one-way verifier compliance ("Grok says change, Fable changes it") and wants peer deliberation, not hierarchy (Dan-directed 2026-07-12).
D-31 (2026-07-12) [harness]: **MEMORY.md hard line cap** — hot-tier router capped at ≤70 lines, enforced mechanically: `away:validate` FAILs (was WARN) when over, including inside `away:batch`; the tripping agent compresses in-session (deletions/compressions always freeze-legal per D-15/D-17) and never raises the cap; no standing condenser agent (Archivist role stays in the deferred Librarian plan, revisit at extraction) — because MEMORY.md drifted to 75 lines and the advisory WARN was ignored across sessions (Dan 2026-07-12; D-16 pain logged). Numbering: D-28–D-30 landed via PR #26 (merged).
D-32 (2026-07-12) [harness]: **Platform parity by default for new rules** — extends D-20: every new rule, gate, loop, script, or workflow binds desktop Windows PC, mobile Cursor, and cloud VM identically from creation; no per-rule parity clause needed (existing clauses remain as documentation, not opt-ins); enforcement mechanics must be client-neutral (node scripts `path.join` + CRLF-normalized `readText()`, npm `prepare` hooks, PR CI); only physically-impossible exceptions may be declared, documented in `AGENTS.md` + `docs/cloud-agent-iphone-setup.md`; mobile parity = alwaysApply rules + cloud-VM execution mechanics (phone runs no local git/hooks); a rule working on one client only is a bug, not a variant — because Dan directed that all rules created today (D-28–D-31) and all future rules be assumed cross-platform rather than opting in per rule (Dan 2026-07-12).
D-33 (2026-07-12) [harness]: **Question deliberation — orchestrator presents** — any Dan question that dispatches a qualifier/verifier (Q&A Verifier, Planning Verifier, or any verifier consulted to shape an answer) requires the drafting agent and verifier to deliberate as peers until genuine agreement on the best answer (findings are proposals; adopt or rebut with reasoning; verifier explicitly accepts/rejects each rebuttal; existing max cycles unchanged); the final note to Dan is always authored and delivered by the orchestrating agent (currently Composer 2.5) in its own words, never raw verifier output; unresolved after max cycles → present both positions with a `Caveat:`; parity automatic per D-32 — because Dan wants question answers to emerge from genuine model deliberation with one consistent voice presenting, not one-way verifier verdicts pasted through (Dan 2026-07-12).
D-34 (2026-07-12) [harness]: **Trust over cost** — the end goal of model routing is trusted output, not saved money: a cheap model whose output cannot be trusted is pointless; the product is cheap worker + paired verifier reaching genuine agreement. Default pattern for all verifier-paired work on all clients: route to cheapest appropriate worker → pair with routed verifier lane → deliberate as peers until both agree (D-30/D-33 mechanics, existing max cycles) → only then the orchestrator (currently Composer 2.5) replies to Dan; extends D-33 from questions to all verifier-paired work; tier table = trust calibration (escalate when cheap-tier agreement fails, not because costlier is better); trivial-skip carve-outs unchanged as trust judgments; no unverified cheap output presented as done where a verifier trigger fired; parity automatic per D-32 — because Dan directed that trust, not cost saving, is the purpose of the routing system (Dan 2026-07-12).
D-35 (2026-07-13) [harness]: **Environment auto-detection — device declarations removed** — agents determine execution environment mechanically (local Windows PC `C:\Projects\stageverify` vs cloud VM `/workspace`; phone = viewport onto cloud agents, not a third environment); Dan never says "I'm on my phone/mobile" — such statements are ergonomic context only and MUST NOT act as routing/tier/ship-workflow/rule triggers; "on my phone"/"iPhone" removed from D-27 mobile-ui-ship trigger phrases (trigger = phone-first product surface, task class only); environment-dependent behavior keys off detected environment; rules propagate to both environments automatically via repo commit + pull (D-32) — because Dan directed that the system must know where it runs without being told, and device mentions were doubling as false routing signals (Dan 2026-07-13).
D-36 (2026-07-13) [harness]: **Solution deliberation before implementation** — direct Dan work directives whose diff would be a substantive ship (Ship Verifier path class) require Composer to draft a solution proposal (approach, files, risks, alternative) and reach Grok Solution Verifier agreement via peer deliberation (D-30/D-33 mechanics, max 2 cycles) BEFORE the first edit; trigger requires a real solution choice (T1+/multi-file/branching within ship path class); unresolved → STOP and present both positions to Dan (never implement through disagreement, per D-30) or escalate architecture per ladder; T0-skip claims post-hoc falsifiable by Ship Verifier (multi-file diff under a single-file skip = FAIL finding); skips as trust judgments: T0 single-file unambiguous, excluded ship paths, repair/planning/Q&A-owned loops, away items with approach-level approved specs, Critical-Reviewer-subsumed (never both on one decision); evidence `solution-verifier:` line mandatory, missing = NOT RUN; announce-and-go redefined as Dan-nonblocking (never Grok-skip); post-ship Ship Verifier unchanged and now verifies execution matched the agreed solution — because Dan directed that Composer must propose and deliberate with Grok before doing, not only after shipping (Dan 2026-07-13; D-16 Dan-directed reopen, pain logged).
D-37 (2026-07-17, amended 2026-07-19, amended 2026-07-23) [harness]: **Dispatcher verifies + implementer lock** — implementation work is always dispatched to Composer (orchestrating agents never edit product/code themselves when Composer is available); **non-Composer parents MUST `Task({ model: "composer-2.5-fast" })` for builds — never inline implement and never `resume: "self"` for product code** (D-43); the agent that dispatched Composer MUST itself verify the returned work before ship/present (diff-vs-scope read, confirm worker-reported PASS on named `verify:*`/Playwright by exact script name, route normal verifier lanes including Build Checker before commit); dispatcher MUST NOT re-run the same `verify:*`/Playwright when worker evidence is complete unless evidence missing/ambiguous, scope drift, prior FAIL in cycle, or Dan asks recheck — purely additive on verifier lanes (security gate, Ship Verifier, fix-closure unchanged); mechanical verify re-run deduped when worker Playwright PASS is evidenced (2026-07-19 amend) — because Dan observed the orchestrator doing work directly (2026-07-12/13) and a Grok parent self-fork implementing Catch-all delivery (2026-07-23); directed: work goes to Composer, the caller verifies it.

D-38 (2026-07-18, amended 2026-08-01) [harness]: **Security gate = Sonnet 4.6 allowlist only** — locked two-slug preference (**`claude-4.6-sonnet-medium-thinking` first**, then `claude-4.6-sonnet-high-thinking`); first-accepted dispatch; UI **"Couldn't start"** / Invalid model = rejection → retry next slug (`fallback-from:`); non-allowlist = NOT RUN; **if both slugs platform-rejected → STOP (no silent Sonnet 5 / Opus / Composer)**; Sonnet 5 only via Dan one-shot + verifier-log calibration + `ALLOWED_GATE_MODELS` same commit — because high-thinking often fails Task start on current Cursor allowlist while medium works (Dan 2026-08-01 subagent start failures; original D-38 2026-07-18).
D-39 (2026-07-20) [product]: **One delivery row per logical invoice** — Approve always creates shell `delivery-vii-{importId}`; Link-to-existing removed (CF rejects `link` and approve+`deliveryOrderId`); multi-page same Invoice # coalesces to one import even beside 1-page siblings; View original PDF opens the full source/org PDF for the batch; already-shared non-shell links use **Create separate delivery** (`relink_to_shell`) — because match-link made multiple approved invoices share one Deliveries row (Dan 2026-07-20).
D-40 (2026-07-22) [product]: **Technician door = any location QR → tech PIN → directed spot list (always-strict day-release)** — scan any permanent `#/s?loc=` QR (entry gate only); PIN unlocks spots/jobs dispatch released for that tech+day; empty releases → "Nothing released for you yet" (never fail-open to all ready jobs); scanned spot is badge/sort hint only, not a content filter; same `JobPickupScreen` as token door; BuildOps paste URL remains fallback — because BuildOps pasted pickup URLs are often untappable and techs need "where to go," not "what's at this bay" (Fable+Composer 2026-07-22; amends location-first Phase 5).
D-41 (2026-07-22, amended 2026-07-24 by D-49) [product]: **Phase 6 Slice A — catch-all intake + parcel identification (door phone out of SV)** — dispatcher assigns a dedicated catch-all staging spot + enables parcel intake; office/management scans that location QR + **management PIN** (multi-PIN + capability matrix per D-49; was single shared PIN) → sees jobs waiting for parts when permitted; packing-slip checkmark **directly marks the expected delivery received** via a gated, logged CF (narrow exception to flag-only — not a silent status edit); unidentifiable parcels → flagged shell (`reviewFlag`); **sequencing: after Phase 5 Slice A lands**, without blocking in-flight Phase 5 Slice B — because office needs same-day parcel ID + receive without a door phone or dispatcher round-trip (Fable recommend `7e98f506-0649-42cb-b6d2-848cb34609a5`; amends location-first Phase 6).
D-42 (2026-07-22) [harness]: **UI ships require Playwright readable-text/contrast check** — any visible UI edit in `src/` must pass a route verify with mechanical contrast assert (`scripts/lib/ui-text-contrast-lib.mjs`, fail on white-on-white / low WCAG ratio), not build-only or screenshot-only; first concrete route = Settings Technicians (`verify:settings-technicians`); other UI ships extend `scripts/verify-*.mjs` with the same helper — because repeated white-on-white Settings text shipped without automated catch (Dan 2026-07-22 Phase 5 Slice A phone test).
D-43 (2026-07-22, amended 2026-07-23, amended 2026-07-24 by D-50) [harness]: **Composer↔Grok collab — four-phase build lock (every conv)** — (1) Composer proposes → Grok Solution Verifier agrees (D-30/D-36); (2) **Composer only** implements (**D-50 carve-out:** Grok implements after 3rd-fail stuck-task Agree until that ship closes); (3) Grok **Build Checker** PASSes on **uncommitted** diff vs agreed solution **before `git commit`**; (4) **Composer only** commit + push + deploy (`ship-loop.mdc`), then Grok Ship Verifier post-push (+ Repair on repair intent); **ship lock:** Grok **never** commits, pushes, or deploys; **implementer lock:** non-Composer parents must Task `composer-2.5-fast` — never Grok/`resume: "self"` product builds (D-37) except D-50 stuck scope; T0 Solution Verifier skips unchanged; Build Checker falsifies bogus T0 skips when final diff is multi-file/branching; evidence `solution-verifier:` + `build-checker:` + `ship-verifier:` + when D-50 `stuck-task-agree:` + `stuck-implementer:`; alwaysApply via `.cursor/rules/composer-grok-collab.mdc` + `model-gates.mdc` — because Dan directed collaborate-then-Composer-codes (2026-07-22) and after a Grok self-fork build attempt required Grok pre-commit check that Composer built it right (Dan 2026-07-23); D-50 adds automatic 3-fail Grok implement after Grok↔Sonnet Agree (Dan 2026-07-24 owner reopen).
D-44 (2026-07-22, amended 2026-07-22; PIN auth amended 2026-07-24 by D-49) [product]: **Phase 6 Slice C — catch-all arrival notify (email-first MVP)** — when a box is physically at the dispatcher-assigned catch-all spot (door phone out of SV), authenticated dispatcher clicks **Notify catch-all checkers** (Settings Management section or dashboard catch-all card when `parcelIntakeEnabled`; **not** office-side `ManagementCatchAllHub` / `getManagementWaitingParts`) → gated CF `notifyCatchAllCheckers` emails active `officeReceivers` with `catchAllCheckInEnabled` + `notifyEmail` (message: spot label/code, shop-local timestamp — **alert only**; optional `#/s?loc=` convenience link, not required primary path); **office check-in entry (authoritative):** scan **any** location QR → **management PIN** (capability matrix per D-49) → **Catch-all check-in** button (when `catchAllCheckIn` cap) → waiting-parts hub (`getManagementWaitingParts`); designated catch-all spot remains for physical drop + notify copy; **Notify ≠ Arrived** / `markCatchAllDeliveryReceived`; identity = new `officeReceivers` collection (dispatcher-only read/write rules like `technicians` — PII, no public read); check-in auth is **management PIN registry** (notify targets are not Firebase Auth users; `officeReceivers.catchAllCheckInEnabled` is notify eligibility only — unrelated to PIN cap `catchAllCheckIn`); Settings **Office receivers** panel at provisioning (name, email required when email notify; phone/`notifySms` store-only with SMS toggle disabled until Twilio approved); MVP = generic ping (optional dispatcher note later); 15-min shop cooldown via CF-only `catchAllNotifyLog` (never cooldown/recipient state on public `appSettings`); Notify disabled/no-op when Gmail not connected; SMS/Twilio deferred (high-risk CF + Dan secrets approval); Sonnet gate + explicit Dan approval before rules/CF deploy — because office needs a dispatcher-triggered “come check in” signal after physical arrival without a door-phone product surface; checkers must not depend on tapping notify links (Dan 2026-07-22 correction; Slice B audit walk unchanged).
D-49 (2026-07-24) [product]: **Management PIN capability matrix (multi-PIN)** — Settings Management lists named management PINs (`managementPins` CF-only); each PIN has capabilities `enterPortalAnyQr`, `catchAllCheckIn`, `viewWaitingParts`, `markOrFlagParcel`; `verifyManagementPin` matches any active unique PIN, refuses mint without `enterPortalAnyQr`, stamps `pinId`+permissions on `managementSessions`; CF re-reads registry on each assert (deactivate/downgrade mid-session denies); client gates Catch-all CTA / mark-flag UX only; `officeReceivers` stay notify-only; vendor/tech PIN systems not unified; amends D-41/D-44 single shared PIN — because Dan wants assignable capabilities per PIN (e.g. 1234 = full management portal) (Dan 2026-07-24).
D-45 (2026-07-23) [harness]: **UI Playwright Verifier (Grok owns Playwright judgment)** — extends D-42/D-43: on any visible UI edit in `src/`, Grok **UI Playwright Verifier** (`cursor-grok-4.5-high-fast`, readonly) runs/re-runs affected route `verify:*` + D-42 contrast/overlap asserts **before commit**; FAIL → file:line fix list for Composer → same Grok re-verifies until PASS (max 3 cycles, fix-closure); distinct from Build Checker (diff-vs-scope) and Ship Verifier (post-push); Composer may run verify to iterate but never self-closes Grok UI findings; evidence `ui-playwright-verifier:` + PASS required on UI ships; alwaysApply via `composer-grok-collab.mdc` + `model-gates.mdc` + `composer-orchestrator.mdc` § UI Verification Protocol — because Catch-all top-bar ship broke prod header layout and Composer was declaring UI done without Grok Playwright judgment (Dan 2026-07-23).
D-46 (2026-07-24, conf scope superseded by D-47) [harness]: **Pre-implement confidence gate (≥97%)** — classify via MODEL_DOSSIER + agent-ops (archetype, model, confStart); Solution Verifier agreement on substantive product work; session log of archetype/model/conf as it rises; alwaysApply `.cursor/rules/confidence-gate.mdc` cross-linked D-36/D-43 — because Dan directed raise conf before code (Dan 2026-07-24). **Which edits require 97%:** see D-47 (every edit).
D-47 (2026-07-24) [harness]: **Every edit ≥97% confidence (authoritative conf scope; widens D-46)** — before **any file edit** (code, verify scripts, rules, docs, PROJECT_STATUS): classify → scouts/reads/focused Dan question until **conf ≥ 97%** → then edit; Solution Verifier (D-36) unchanged on its own triggers and N/A carve-outs; sole conf-bar skip = mechanical `package.json` version patch in the same ship commit as work already at ≥97%; alwaysApply `.cursor/rules/confidence-gate.mdc` + pre-edit dispatch in `model-gates.mdc` — because Dan directed **"I want the 97% rule to be followed with every edit"** (Dan 2026-07-24). **Solution Verifier timing when required:** D-48.
D-48 (2026-07-24) [harness]: **Solution Verifier PASS before first edit (pairs with D-47)** — when D-36 requires Solution Verifier, **PASS** (or valid N/A) and in-session evidence (`solution-verifier:` + model) must precede the **first repo write** in scope (any path — no docs/rules-first Agree dodge) — not after implement, not completion-report-only, not deferred from worker to parent after the worker already edited; workers dispatch Agree themselves or return without edits; parent must not accept implement-first shortcuts; missing = NOT RUN = do not ship as done; alwaysApply via `confidence-gate.mdc`, `composer-grok-collab.mdc` § Pre-edit gate ordering, `model-gates.mdc` § Solution deliberation — Dan-directed reopen (owner authority); 2nd logged incident: print-label worker edited then ran Solution Verifier (Dan 2026-07-24).
D-50 (2026-07-24, amended 2026-07-25 by **D-55**) [harness]: **3-fail stuck-task escalation (automatic)** — after **two consecutive fails** on the same task scope, **3rd try = D-50** (any fail type per D-55; not only implement+verify): fails 1–2 = Composer retry then 2nd-fail lane (D-19 stall-advisor on 2nd **same** fingerprint, Sonnet diagnose on 2nd **different** fingerprint); **3rd try** = Grok ↔ Sonnet 4.6 peer Agree on root cause + exact fix plan (Solution-style; Sonnet diagnose/Agree role; security gate slug unchanged before push if high-risk) → **Grok implements** that stuck scope only (carve-out to D-43/D-37 implementer lock) → normal UI Playwright / Build Checker / **Composer** commit+push+deploy / Ship Verifier → **Composer resumes** default implementer for next work; evidence `stuck-task-agree:` + `stuck-implementer:`; alwaysApply `model-gates.mdc` § 3-fail + `composer-grok-collab.mdc` — because Composer-only retries spun on repeated verify failures without a stronger Agree+implement pivot (Dan 2026-07-24 owner-directed reopen; pain logged HARNESS_V1_FREEZE). *(Dan conversation label "D-49" for this harness preference — registry **D-49** already assigned to product Management PIN matrix.)*
D-51 (2026-07-24) [harness]: **UI before/after screenshot gate (mandatory on visible UI)** — before first `src/` UI edit capture `before-<route>.png` per affected route; after clean build capture `after-<route>.png`; agent must compare (intended change visible + no nearby chrome regressions e.g. top-bar buttons); missing compare = `ui-before-after: NOT RUN` = no commit; delete ephemeral PNGs before push; **complements** Playwright + D-42 contrast + D-45 Grok UI Playwright Verifier (does not replace); alwaysApply `.cursor/rules/ui-before-after.mdc` + orchestrator § UI Verification Protocol — because Catch-all delivery header button disappeared after a ship declared done on build/verify alone (Dan 2026-07-24; pain logged HARNESS_V1_FREEZE).
D-52 (2026-07-25) [product]: **Settings staging spots = Staging Map SSOT** — Settings Workflow staging list shows **one row per visible map layout slot** (`visibleShopMapLayoutSlotKeys` / `filterStagingLocationsOnShopMap`): all G spots, shelf slots (S*n*-*letter*), and Catch-all (CA) when the catch-all marker is on the map; Firestore zone docs hydrate label/type/QR when present, and slots without a zone doc still appear with map slot code + default label; add/remove spots on the map (Edit Locations) is the sole path to add/remove list rows; Settings no longer creates orphan zones off-map — because Dan directed full map/list parity (Dan 2026-07-25; follow-up same day).
D-53 (2026-07-25) [product]: **Staging location list sort order (SSOT)** — any UI list of staging locations (Settings Workflow staging spots, batch print-label picker, Management catch-all spot dropdown, exports) sorts **Catch-all first** (when on map / configured), then **ground G1→G12** (numeric), then **shelf S1-*→S2-*…** (unit then letter); single comparator in `src/dispatcher/stagingMapSync.ts` (`compareStagingMapLayoutSlots` / `compareStagingLocationsForList`); cross-link D-52 map membership — because Dan directed stable walk/print order (Dan 2026-07-25).
D-54 (2026-07-25) [harness]: **Ship Verifier same-fingerprint FAILs → 2-fail/D-50** — consecutive Ship Verifier FAILs on the same task/ship scope count toward § 2-fail + D-50 (with implement+verify fails); normalized fingerprint for mandatory evidence gaps = `ship-verifier + missing:<ui-playwright|build-checker|ui-before-after|security-gate|solution-verifier>` (+ optional commit range); 1st FAIL = fix-closure; **2nd same fingerprint = mandatory Grok stall-advisor** before another retrospective close; **3rd = automatic D-50 Agree** (no 4th close without Agree); amends D-19 “post-ship Ship Verifier” stall skip; alwaysApply `model-gates.mdc` § Ship Verifier + § 2-fail; platform parity D-32 — because deliveries UX ships hit 3× Ship Verifier FAIL for missing D-43/D-51 evidence without stall/D-50 (Dan 2026-07-25; pain logged).
D-56 (2026-07-26, amended 2026-07-26) [product]: **Johnstone invoice AI shadow (Vertex, Google-only)** — flag `appSettings.config.invoiceAiShadowEnabled` (default false); when on, after regex + `forceReviewOnlyStatus`, CF runs Gemini `gemini-3.5-flash-lite` then **always** validates with `gemini-3.6-flash` (location `global` — not escalate-only); stores structured `aiShadowParse` only (never flips `pending_review` / never auto-ok); qty-only reconcile; vendor playbooks in private GCS `gs://stageverify-db-invoice-training/vendors/{key}.md`; on Invoice Review approve, optional generalized `correctionNote` appends redacted lesson to MD (no invoice #/PO/PII); regex hold N=5 + shadow ≥99% on 5 invoices are promote metrics only (not v1 cutover); Pro rung deferred — because Dan directed outside-Cursor Google teacher loop with human→MD memory + always-on 3.6 validator (Dan 2026-07-26).
D-57 (2026-07-26) [product]: **Invoice training Admin + Save lesson** — Parsed import data **Admin** (left of View original PDF) password-gates full edit of current vendor GCS playbook; **Save lesson** writes Training note without Approve/Reject (works when Approve blocked); Approve+note still appends; toast “Got it — new rule added to internal notes…” on successful write + Admin MD save (keep success banner); safety/redaction reject → pending-Admin UI + CF Gmail to Settings alert email; Settings dispatcher-only section sets alert email + Admin password (scrypt hash in `invoiceTrainingAdminSecrets`, CF verify only; both required before Admin unlock; unset → `#/settings?focus=invoice-training-admin`); password every Admin click — because Dan needs in-app teach/remember + reviewable MD without Cursor (Dan 2026-07-26 overnight away-136).
D-58 (2026-08-01, amended 2026-08-03) [product]: **Parsed import ignore — pending by default; opt-in by taught fingerprint** — Imports stay `pending_review` until a human teaches ignore via Parsed import teach-chat (echo → yes); system fingerprints the open doc (`vendorKey` + `parserFormatId` + `documentType`) into Firestore `vendorInvoiceIgnoreRules/{vendor}__{format}__{type}` (legacy credit boolean migrated to `credit_memo`); Admin edits in Settings → Invoice training Admin (password-gated CF → Firestore); ingest auto-skips only **new** imports matching an enabled fingerprint; Re-open stays pending and is not re-auto-skipped on refresh; never arm `unknown-vendor`; invoice document type requires stronger echo warning before arming; **ignore = review-queue skip only** — never deletes deliveries, items, or other business data, and never auto-approves; GCS playbook MD remains separate AI tips — because Dan needed ignore for any curveball document type, not CREDIT/returns only (2026-08-03).

D-55 (2026-07-25) [harness]: **Two consecutive fails → third try is D-50 auto-escalate (any fail type)** — on the same task/ship scope (session or continued thread): 1st fail → Composer retry; 2nd consecutive fail → stall-advisor when **same fingerprint** (D-19/D-54), Sonnet diagnose-only when **different fingerprint** — **2nd fail always counts**; **3rd try = mandatory D-50** (Grok ↔ Sonnet Agree → Grok implement per D-50) — **not** another Composer-only implement+verify, Ship Verifier retrospective close, or build/Playwright/verify-fix loop; **fingerprint match not required** to trigger D-50 on the 3rd try (widens/clarifies D-50 + D-54 — closes loopholes for mixed-fingerprint spin); alwaysApply `model-gates.mdc` § 2-fail + § 3-fail + § Ship Verifier + `composer-orchestrator.mdc` § Escalation; platform parity D-32 — because Dan standing preference: no third ordinary retry after any two consecutive failures (Dan 2026-07-25).

D-59 (2026-08-03) [product]: **Training-note hardening spec (Fable 5 war-game + Dan's 12 decisions)** — Fable 5 read-only war-game of the shipped Training-note / document-ignore lanes (v0.0.195/196, D-57/D-58) returned verdict **REQUIRES PRODUCT DECISIONS BEFORE SPECIFICATION**; Dan decided: (1) new **manager role** activates ignore rules — dispatcher proposes only; (2) **no shadow mode** — rules live immediately on manager activation; (3) **circuit breaker**: auto-disable a rule after **2 admin re-opens** of docs it skipped; (4) `unknown` documentType/parserFormatId **never armable** — unparseable docs always stay in review; (5) rules pin to **sender email domain(s)** (header From, max 5) before auto-skipping; (6) docs with **strong invoice signals** (parsed invoice #, amount-due wording) **never auto-ignored** — always Needs Review (consequence: `invoice` type not armable); (7) structured-lesson MVP category = **fulfillment-method mapping only**; (8) structured interpretation engine **deferred** — playbook prose stays; fulfillment mapping is the first category when that future phase (P8) gets its own spec; (9) raw training notes retained **90 days** in admin-only `trainingNoteAudit` (Firestore TTL); (10) immutable rule-lifecycle audit stream (`ignoreRuleAuditEvents`) + rule delete becomes **soft-delete/archive**; (11) **redaction preview** shown before lesson save; (12) keep D-57 Approve+note conflation (with UI hint). Spec: **`docs/training-note-ignore-spec.md`** — 8 phases (P1 server-echo propose/confirm + never-unknown first; **P2 manager role high-risk entirely** — Dan pre-approval on grant mechanism; P5/P7 firestore.rules diffs high-risk; every CF deploy high-risk + Sonnet gate; Fable phase-boundary review after P1/P2/P5/P7). Known-limitation follow-up queued as **away-137** (blocked until D-59 P1–P7 complete; hot-tier `CURRENT_STATE` Queued product): `firestore.rules:67-77` allow any authenticated client to write `deliveries`/`items` directly — because Dan wants the training box safe-by-architecture (untrusted note → constrained effect only) before it generalizes, then that write-boundary fixed next (Dan 2026-08-03; war-game agent 8f7e934d; queue follow-up Dan 2026-08-03). **P3 (2026-08-04):** spec §30 open item #2 closed — grandfathered domain-less active rules get a **7-day grace** (`domainGraceStartedAt`) before stop-matching; Settings flags grace vs expired; manager activate requires ≥1 domain. **P4 (2026-08-04):** strong-invoice-signal guard — `hasStrongInvoiceSignals` blocks auto-skip when parsed invoice # or §13 amount-due wording; `ignoreRuleSuppressedBy: "strong_invoice_signals"` on pending near-misses; review UI chip + inspect banner (no P5 audit events). **P5 (2026-08-04):** `ignoreRuleAuditEvents` immutable stream + `matchedRuleId` on auto-skips + matchCount/lastMatchedAt; hard delete removed (archive-only); legacy vendor-only rules migrated idempotently; Settings show-archived + audit drill-in + manager migration button. **P6 (2026-08-04):** circuit breaker — `reopenCount` on rule doc; reopen of `system:document_ignore_skip` imports increments count; at **2** active rules auto-disable (`disabledReason: auto_false_positive`); audits `match_reopened` + `auto_disabled_false_positive`; manager bulk `bulkReopenImportsSkippedByRule`; manual rejects do not increment. **P7 (2026-08-04):** `trainingNoteAudit` (90-day `expireAt`, CF-only; **enable Firestore TTL on `expireAt` in console at deploy**); `previewTrainingLessonRedaction` + inspect preview dialog; **N=20** lessons/hr/uid (`MAX_TRAINING_LESSONS_PER_HOUR_PER_UID`); ignore-lane note on `confirmVendorIgnoreRule`; Settings audit list callable.

D-60 (2026-08-05) [harness]: **High-risk Sonnet instruct→verify loop** — on high-risk ships (same trigger class as ship-loop: rules, CF deploy/writes, auth/route guards, secrets, schema migrations): after Dan approval, Sonnet 4.6 (D-38 allowlist) drafts instruction packet → Composer ↔ Sonnet Agree **before first product edit** (`sonnet-instruct:`); Composer implements (D-50 Grok implement carve-out unchanged); Sonnet verify loop reviews diff vs packet + judges named acceptance `verify:*`/`test:*` PASS — blocking before commit (`sonnet-verify:`); two consecutive Phase-3 FAILs → D-50; subsumes D-36 Solution Verifier (`solution-verifier: N/A (high-risk — D-60 sonnet-instruct)`); Grok D-45 UI Playwright **N/A** (Sonnet owns Playwright on high-risk); Build Checker + D-38 pre-push security gate + Ship Verifier unchanged in order after Phase 3 PASS; alwaysApply `.cursor/rules/high-risk-sonnet-loop.mdc`; platform parity D-32 — because Dan directed Sonnet-in-the-loop beyond pre-push gate alone for auth/CF/rules (Dan 2026-08-05 owner-directed reopen; pain logged HARNESS_V1_FREEZE).
