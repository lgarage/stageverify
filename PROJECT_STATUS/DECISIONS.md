# Decision Registry

## How this file works
- Active section capped at ~40 decision lines; one line per decision.
- Format: `D-NN (YYYY-MM-DD) [harness|product]: <decision> — because <compressed why>.`
- Stable IDs — never renumber. Superseded → `PROJECT_STATUS/DECISIONS_ARCHIVE.md` with `superseded-by D-NN`; never delete.
- `[harness]` = agent/orchestration; `[product]` = StageVerify behavior/architecture.
- Record new decisions in the **same commit** as the work that implements them.

## Active decisions

D-01 (2026-07-09) [harness]: Two-tier ship model — fast-safe (`src/`, `public/`, verify scripts, docs, PROJECT_STATUS, routine service logic) build/commit/push/deploy without approval; high-risk (auth, `firestore.rules`, CF write paths, secrets, billing, data deletion, schema migrations, deploy config, auth/route-guard files under `src/`, root `package.json` deploy wiring) require explicit Dan approval before implementation **and** deploy — because bad backend deploys are not cheaply revertible like a bundle; ambiguous tier → classify high-risk and ask.
D-02 (2026-07-09) [harness]: Tiered verification ladder — Grok 4.5 Fast = post-ship ship-verifier (scope/correctness) and pre-decision critical reviewer (plans/architecture); Sonnet 4.6 = mandatory security gate (unchanged); Fable 5 = work verifier for tier-3/phase-boundary work only — because each check should use the cheapest capable model.
D-03 (2026-07-09) [harness]: Evidence standard — every review/verification claim requires subagent Task ID + model line in the completion report; missing evidence = NOT RUN — because reviews were previously claimed but never executed.
D-04 (2026-07-09) [harness]: Universal fix-closure — whichever model reported an issue re-verifies the fix before closure — because fixes were being closed without the reporter confirming them.
D-05 (2026-07-09) [harness]: Lessons compression — lessons files keep only compressed reusable rules that change future behavior; specific incidents get distilled then archived — because per-incident logs are context bloat.
D-06 (2026-07-09) [harness]: Trim always-applied rules in place; defer flipping time-awareness / parallel-agent-strategy to on-demand until attachment reliability is proven — because heuristic attachment risks silent behavioral regression (Grok review finding).
D-07 (2026-07-09) [harness]: Away/overnight workflow follows the same ladder — per-item ship verification, queue-time high-risk `danApproved` schema check, mandatory verifier report lines — because away batches must not bypass harness gates.
D-08 (2026-07-09) [harness]: Decision registry + handoff protocol — decisions recorded same-commit; "prepare for new conv" flushes in-flight state; new conversations bootstrap from `CURRENT_STATE.md` + this file — because handoff prompts should never need to be hand-written.
D-09 amended (2026-07-20): Vendor PIN remains job-scoped by default (D14). Per-vendor `companyWideSessionEnabled` (dispatcher-set, default false) unlocks a separate company-PIN path: after any location QR, the vendor company PIN creates a vendor-scoped session listing that vendor's open orders across jobs; job PINs never elevate and stay job-scoped. Each checklist row is two lines (job name; spot · invoice · PO); rows without an assigned/planned spot cannot be checked or bulk-delivered (client + server). Bulk Delivered requires a confirm dialog listing distinct jobs; overflow suggestions stay empty-unassigned-only; auto-confirm timeout deferred to a separately approved ship. StageVerify makes each job and assigned spot unmistakable and surfaces misses for coaching; wrong-bay accountability remains with the vendor to train drivers.
D-10 (2026-07-09) [product]: Printed QR strategy is static permanent location signs (`#/s?loc={code}`); occupancy-dynamic QR-flip rejected; `qr-routing`, `zone-lookup`, `encode-qr` dossier § refreshed — legacy dispatcher zone e-tags still emit `#/receive?` / `#/pickup?` until Phase 3 role-aware resolver ships — because location-first signs must never change when occupancy changes.
D-11 (2026-07-09) [product]: Canonical vendor UI is `ReceivingPage` at `/#/receive` only; legacy `/#/` and `/#/checkin/:id` (plus compact `#/r?`) redirect there — because duplicated vendor UIs caused scan/routing drift.
D-12 (2026-07-09) [harness]: Fable 5 never implements — all file changes go to Composer 2.5; Fable verifies the result and gates ship (ship only after Fable confirms correct, with fixes looping back to Composer per D-04) — because implementation on the expensive model wastes cost and blurs the verify role.
D-13 (2026-07-09) [harness]: Handoff bootstrap is trust-based — new conversations read CURRENT_STATE.md + DECISIONS.md only; no transcript mining, no re-verifying recorded verdicts (handoff flush must record verdict + commit hash per item) — because re-deriving verified state from primary sources burned 37% of a session's tokens and 7 minutes.
D-14 (2026-07-09) [harness]: Autonomy-evidence pairing — no new autonomy without evidence produced for a non-author; evidence means report lines/logs/verdicts, not infrastructure (dashboards must justify separately) — because authority without independent observation is how claimed-but-never-run reviews happened.
D-15 (2026-07-09) [harness]: Organizational concepts must delete existing complexity in the same commit or be rejected; capability additions instead face the 80/20 burden-of-proof (rejected when rare-case value < permanent complexity tax; catastrophic-asymmetry gates like the security gate are exempt) and must name a maintenance owner — because concepts that delete nothing become permanent layers.
D-16 (2026-07-09) [harness]: Harness V1 Freeze — asymmetric (deletions always legal; additions need a named dated pain event ≥2×, a security/production incident, a measured bottleneck, or a real customer gap); charter: PROJECT_STATUS/HARNESS_V1_FREEZE.md — because further harness work now delays StageVerify MVP (CTO decision, adversarially reviewed rounds 1-5).
D-17 (2026-07-09) [harness]: Fable 5 role is CTO for StageVerify delivery — burden of proof flipped: any harness proposal must justify delaying product; Fable actively rejects speculative harness/architecture/automation work per the V1 Freeze (D-16) and keeps effort on MVP — because Dan is naturally attracted to improving systems and delegated the guard role.
D-18 (2026-07-10) [harness]: Auto-gotcha learning — Phase 0–1 extend `classifyVerifyFailure` + manual `gotcha-map` entry + demo regressions; pending→indexer on `away:ship` unchanged; Phase 2 auto `--apply-gotcha` and broadened packet injection require explicit Dan approval — because verify auto-capture logged failures but misclassified spawnSync patch hangs (missing `process.exit`) and never promoted to session-start memory (Grok verify `0c002bcb`, plan `537f0ed9`).
D-19 (2026-07-10) [harness]: Consecutive same-failure Grok stall-advisor — 2nd same fingerprint on same task dispatches one read-only Grok (`grok-4.5-fast-xhigh`, `generalPurpose`, `readonly`) for hypotheses/next experiments; Sonnet diagnose-only when still stuck (different fingerprint on 2nd fail, or 3rd+ fail after Grok) — because repeated identical verify/build failures wasted cycles without a cheap pivot (Dan 2026-07-10 consecutive same-failure stall; D-16 pain logged).
D-20 (2026-07-11, amended) [harness]: **Platform parity** — `.cursor/rules/*` alwaysApply rules bind all Composer sessions (desktop, mobile Cursor, cloud VM) with identical harness: ship-loop tiers, repair loop, planning verify loop, verification ladder, security gate, stall-advisor, verifiers, evidence lines, fix-closure; no mobile-lite orchestration; exceptions only where physically impossible (secrets UI, branch-only ship) documented in AGENTS.md + cloud setup — repair loop detail: on Dan repair intent route to correct verifier (Grok default; Sonnet security/auth/rules/CF; Fable spec-phase; D-19 unchanged); verify → Composer fix → same verifier re-verify until PASS (max 3 cycles); evidence `repair-verifier:` + `fix-verified:`; no self-close — because Composer was self-closing repairs and mobile was treated as lite harness (Dan 2026-07-11; D-16 pain logged).
D-21 (2026-07-11) [harness]: **Planning verify loop** — on planning triggers ("what's next", "what else can be worked on", "what can we work on", roadmap/priority, ranked options, away planning) scouts synthesize → Grok Planning Verifier (`grok-4.5-fast-xhigh`, `generalPurpose`, readonly) cross-checks authoritative sources → PARTIAL/FAIL → Composer revises → same Grok re-verifies until PASS (max 3 cycles); only Grok-PASS answer presented; evidence `planning-verifier:` + `fix-verified:`; platform parity with D-20 — because Composer first-draft planning answers omitted recent ships, invented away IDs, and contradicted CURRENT_STATE (Dan 2026-07-11; D-16 pain logged).
D-22 (2026-07-11) [harness]: **Q&A verify loop** — on non-trivial Q&A (how/why/where/explain/recommend/clarify) on all clients (desktop Windows PC, mobile Cursor, cloud VM) Composer drafts → Grok Q&A Verifier (`grok-4.5-fast-xhigh`, `generalPurpose`, readonly) cross-checks cited sources → PARTIAL/FAIL → Composer revises → same Grok re-verifies (max 2 cycles); evidence `qa-verifier:` + `fix-verified:`; skip trivial single-fact and planning triggers (D-21); platform parity with D-20 — because Dan wants Grok conferral on Q&A answers for accuracy, not only planning (status wording confusion 2026-07-11; Dan explicit).
D-23 (2026-07-11) [harness]: **Doc drift validate + hot-tier auto-sync** — `verify:location-phaseN` prod PASS (Snapshot/Verify) or ship note → auto-write CURRENT_STATE gate closure + Phase Tracker + roadmap; `away:validate`/`away:ship`/`away:sync --write`; validate fails only if auto-sync cannot fix; Dan never hand-edits phase close; **platform parity D-20** (desktop Windows PC + mobile Cursor + cloud VM identical `node` scripts) — because planning agents read stale docs (2× pain; D-16).
D-24 (2026-07-12) [product]: **MVP path SSOT** — `PROJECT_STATUS/MVP_PATH.md` records 80% completion (Fable+Grok confer 2026-07-12), five exit criteria, and fastest-path priority order; all future planning/progress sessions must analyze the full MVP landscape (§14 + roadmap traceability + blockers), not queue head alone, and present the fastest clear path to MVP done — because queue-empty sessions were drifting to harness/phase work while §14 E2E and live email gates remained open.
D-25 (2026-07-12) [harness]: **MVP completion % reporting** — all agents on all clients report `MVP completion: XX.XX%` (2 decimals) in substantive work replies when task traces to `MVP_PATH.md`; update SSOT on milestone ship; skip dedicated line for pure planning (planning still leads with %); rule `.cursor/rules/mvp-completion-report.mdc` alwaysApply; repo commit = PC rule sync — because Dan needs continuous MVP progress visibility without re-asking.
D-26 (2026-07-12) [product]: **E-tags / ESL excluded from MVP scope** — e-tags, Minew ESL, §14 step 26, and all ESL automation are **not** in MVP %, exit criteria, fastest path, or active blockers; post-MVP product track only (`ESL_INTEGRATION_PLAN.md`); revisit after MVP operational with requirements restated + verify/tests — because Dan directed zero e-tag work toward MVP done.
D-27 (2026-07-12) [harness]: **Mobile UI merge+deploy** — when Dan asks for a mobile UI change (vendor check-in, pickup portal, phone-first public routes), agents **always** merge to `main` and `npm run deploy` after build + Playwright pass — no separate “merge/deploy” prompt; overrides cloud branch-only default for that class; high-risk/verify-fail gates unchanged — because Dan validates on iPhone and branch-only ships blocked phone testing (Dan 2026-07-12).
D-28 (2026-07-12) [harness]: **Mechanical evidence gates + verifier calibration log** — CI gate-check (`.github/workflows/gate-check.yml` + `scripts/gate-check.mjs`) fails PRs whose diffs touch high-risk paths (fail-up: all `functions/` except `lib/`, firestore.rules, workflows, firebase.json, src auth-heuristic paths, root package.json scripts/firebase-major) without `security-gate-id` + Sonnet model line (presence-only — RC-3: not proof of invocation; forks can forge); PR/CI-only — desktop direct-to-main stays honor-system (superseded by D-29); verifier verdicts logged to `PROJECT_STATUS/verifier-log.jsonl` via `verifier:log`, `verifier:stats` = admissible D-16 evidence — because Dan directed freeze reopen (owner authority 2026-07-12) to mechanize prose gates and generate the measurements future freeze decisions need.
D-29 (2026-07-12) [harness]: **Ecosystem-wide evidence gate** — extends D-28 to all clients (same Dan reopen authority 2026-07-12): local pre-push hook (`scripts/install-git-hooks.mjs`, auto-installed via npm `prepare` on every machine) blocks direct high-risk pushes to `main` without the security-gate evidence block in commit messages (`--evidence-from-commits`); zeros-base unified on `origin/main`; `GATE_SKIP=1` / `--no-verify` bypass documented (advisory-vs-malice per D-28); push-on-main CI trigger deferred until merge-commit PR-lookup exists (future addition, own D-16 evidence) — because desktop direct-to-main was the honor-system gap after D-28.
D-30 (2026-07-12) [harness]: **Peer deliberation** — verifier findings are proposals, not orders (verifier ≠ boss); author evaluates each finding, adopts or rebuts with reasoning; both agents reach genuine agreement (or present both positions to Dan) BEFORE the cheap implementer applies changes; operational closure per finding = adopted | rebutted (verifier agreed) | escalated; security HIGH rebuttal allowed but unresolved HIGH still blocks push; disposition line mandatory when findings ≥ 1; fix-closure (D-04) unchanged — because Dan observed one-way verifier compliance ("Grok says change, Fable changes it") and wants peer deliberation, not hierarchy (Dan-directed 2026-07-12).
D-31 (2026-07-12) [harness]: **MEMORY.md hard line cap** — hot-tier router capped at ≤70 lines, enforced mechanically: `away:validate` FAILs (was WARN) when over, including inside `away:batch`; the tripping agent compresses in-session (deletions/compressions always freeze-legal per D-15/D-17) and never raises the cap; no standing condenser agent (Archivist role stays in the deferred Librarian plan, revisit at extraction) — because MEMORY.md drifted to 75 lines and the advisory WARN was ignored across sessions (Dan 2026-07-12; D-16 pain logged). Numbering: D-28–D-30 landed via PR #26 (merged).
D-32 (2026-07-12) [harness]: **Platform parity by default for new rules** — extends D-20: every new rule, gate, loop, script, or workflow binds desktop Windows PC, mobile Cursor, and cloud VM identically from creation; no per-rule parity clause needed (existing clauses remain as documentation, not opt-ins); enforcement mechanics must be client-neutral (node scripts `path.join` + CRLF-normalized `readText()`, npm `prepare` hooks, PR CI); only physically-impossible exceptions may be declared, documented in `AGENTS.md` + `docs/cloud-agent-iphone-setup.md`; mobile parity = alwaysApply rules + cloud-VM execution mechanics (phone runs no local git/hooks); a rule working on one client only is a bug, not a variant — because Dan directed that all rules created today (D-28–D-31) and all future rules be assumed cross-platform rather than opting in per rule (Dan 2026-07-12).
D-33 (2026-07-12) [harness]: **Question deliberation — orchestrator presents** — any Dan question that dispatches a qualifier/verifier (Q&A Verifier, Planning Verifier, or any verifier consulted to shape an answer) requires the drafting agent and verifier to deliberate as peers until genuine agreement on the best answer (findings are proposals; adopt or rebut with reasoning; verifier explicitly accepts/rejects each rebuttal; existing max cycles unchanged); the final note to Dan is always authored and delivered by the orchestrating agent (currently Composer 2.5) in its own words, never raw verifier output; unresolved after max cycles → present both positions with a `Caveat:`; parity automatic per D-32 — because Dan wants question answers to emerge from genuine model deliberation with one consistent voice presenting, not one-way verifier verdicts pasted through (Dan 2026-07-12).
D-34 (2026-07-12) [harness]: **Trust over cost** — the end goal of model routing is trusted output, not saved money: a cheap model whose output cannot be trusted is pointless; the product is cheap worker + paired verifier reaching genuine agreement. Default pattern for all verifier-paired work on all clients: route to cheapest appropriate worker → pair with routed verifier lane → deliberate as peers until both agree (D-30/D-33 mechanics, existing max cycles) → only then the orchestrator (currently Composer 2.5) replies to Dan; extends D-33 from questions to all verifier-paired work; tier table = trust calibration (escalate when cheap-tier agreement fails, not because costlier is better); trivial-skip carve-outs unchanged as trust judgments; no unverified cheap output presented as done where a verifier trigger fired; parity automatic per D-32 — because Dan directed that trust, not cost saving, is the purpose of the routing system (Dan 2026-07-12).
D-35 (2026-07-13) [harness]: **Environment auto-detection — device declarations removed** — agents determine execution environment mechanically (local Windows PC `C:\Projects\stageverify` vs cloud VM `/workspace`; phone = viewport onto cloud agents, not a third environment); Dan never says "I'm on my phone/mobile" — such statements are ergonomic context only and MUST NOT act as routing/tier/ship-workflow/rule triggers; "on my phone"/"iPhone" removed from D-27 mobile-ui-ship trigger phrases (trigger = phone-first product surface, task class only); environment-dependent behavior keys off detected environment; rules propagate to both environments automatically via repo commit + pull (D-32) — because Dan directed that the system must know where it runs without being told, and device mentions were doubling as false routing signals (Dan 2026-07-13).
D-36 (2026-07-13) [harness]: **Solution deliberation before implementation** — direct Dan work directives whose diff would be a substantive ship (Ship Verifier path class) require Composer to draft a solution proposal (approach, files, risks, alternative) and reach Grok Solution Verifier agreement via peer deliberation (D-30/D-33 mechanics, max 2 cycles) BEFORE the first edit; trigger requires a real solution choice (T1+/multi-file/branching within ship path class); unresolved → STOP and present both positions to Dan (never implement through disagreement, per D-30) or escalate architecture per ladder; T0-skip claims post-hoc falsifiable by Ship Verifier (multi-file diff under a single-file skip = FAIL finding); skips as trust judgments: T0 single-file unambiguous, excluded ship paths, repair/planning/Q&A-owned loops, away items with approach-level approved specs, Critical-Reviewer-subsumed (never both on one decision); evidence `solution-verifier:` line mandatory, missing = NOT RUN; announce-and-go redefined as Dan-nonblocking (never Grok-skip); post-ship Ship Verifier unchanged and now verifies execution matched the agreed solution — because Dan directed that Composer must propose and deliberate with Grok before doing, not only after shipping (Dan 2026-07-13; D-16 Dan-directed reopen, pain logged).
D-37 (2026-07-17, amended 2026-07-19) [harness]: **Dispatcher verifies** — implementation work is always dispatched to Composer (orchestrating agents never edit files themselves when Composer is available), and the agent that dispatched Composer MUST itself verify the returned work before ship/present (diff-vs-scope read, confirm worker-reported PASS on named `verify:*`/Playwright by exact script name, route normal verifier lanes); dispatcher MUST NOT re-run the same `verify:*`/Playwright when worker evidence is complete unless evidence missing/ambiguous, scope drift, prior FAIL in cycle, or Dan asks recheck — purely additive on verifier lanes (security gate, Ship Verifier, fix-closure unchanged); mechanical verify re-run deduped when worker Playwright PASS is evidenced (2026-07-19 amend) — because Dan observed the orchestrator doing work directly during 2026-07-12/13 harness sessions and directed: work goes to Composer, the caller verifies it (Dan 2026-07-17).

D-38 (2026-07-18, amended) [harness]: **Security gate = Sonnet 4.6 allowlist only** — locked two-slug preference (`claude-4.6-sonnet-high-thinking`, then `claude-4.6-sonnet-medium-thinking`); first-accepted dispatch; lower only after real Task rejection (`fallback-from:`); non-allowlist = NOT RUN; **if both slugs platform-rejected → STOP (no silent Sonnet 5 / Opus / Composer)**; Sonnet 5 only via Dan one-shot + verifier-log calibration + `ALLOWED_GATE_MODELS` same commit — because v0.0.59–61 used Sonnet 5 when 4.6 was undispatchable and counted as theater (Dan 2026-07-18 evening; Critical Review 9afabac4).
D-39 (2026-07-20) [product]: **One delivery row per logical invoice** — Approve always creates shell `delivery-vii-{importId}`; Link-to-existing removed (CF rejects `link` and approve+`deliveryOrderId`); multi-page same Invoice # coalesces to one import even beside 1-page siblings; View original PDF opens the full source/org PDF for the batch; already-shared non-shell links use **Create separate delivery** (`relink_to_shell`) — because match-link made multiple approved invoices share one Deliveries row (Dan 2026-07-20).
D-40 (2026-07-22) [product]: **Technician door = any location QR → tech PIN → directed spot list (always-strict day-release)** — scan any permanent `#/s?loc=` QR (entry gate only); PIN unlocks spots/jobs dispatch released for that tech+day; empty releases → "Nothing released for you yet" (never fail-open to all ready jobs); scanned spot is badge/sort hint only, not a content filter; same `JobPickupScreen` as token door; BuildOps paste URL remains fallback — because BuildOps pasted pickup URLs are often untappable and techs need "where to go," not "what's at this bay" (Fable+Composer 2026-07-22; amends location-first Phase 5).
D-41 (2026-07-22) [product]: **Phase 6 Slice A — catch-all intake + parcel identification (door phone out of SV)** — dispatcher assigns a dedicated catch-all staging spot + enables parcel intake; office/management scans that location QR + shared management PIN → sees jobs waiting for parts; packing-slip checkmark **directly marks the expected delivery received** via a gated, logged CF (narrow exception to flag-only — not a silent status edit); unidentifiable parcels → flagged shell (`reviewFlag`); **sequencing: after Phase 5 Slice A lands**, without blocking in-flight Phase 5 Slice B — because office needs same-day parcel ID + receive without a door phone or dispatcher round-trip (Fable recommend `7e98f506-0649-42cb-b6d2-848cb34609a5`; amends location-first Phase 6).
D-42 (2026-07-22) [harness]: **UI ships require Playwright readable-text/contrast check** — any visible UI edit in `src/` must pass a route verify with mechanical contrast assert (`scripts/lib/ui-text-contrast-lib.mjs`, fail on white-on-white / low WCAG ratio), not build-only or screenshot-only; first concrete route = Settings Technicians (`verify:settings-technicians`); other UI ships extend `scripts/verify-*.mjs` with the same helper — because repeated white-on-white Settings text shipped without automated catch (Dan 2026-07-22 Phase 5 Slice A phone test).
D-43 (2026-07-22) [harness]: **Composer↔Grok collab default for feature work** — standing preference: for substantive work directives / real feature code, Composer proposes → Grok Solution Verifier peer-deliberates (iterate adopt/rebut until agreement, D-30/D-36) → Composer implements → Grok verifies (Ship Verifier post-ship; Repair Verifier on repair intent); default going forward, not optional for real feature work; trivial skips unchanged (T0 single-file unambiguous, excluded paths, repair/planning/Q&A-owned, approved away specs); no second ladder — strengthens D-36 + `.cursor/rules/composer-grok-collab.mdc` — because Dan directed that Composer and Grok must collaborate on the best code edits before implementation, then Composer codes, then Grok verifies (Dan 2026-07-22).
